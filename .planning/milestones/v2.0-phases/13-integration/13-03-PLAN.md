---
phase: 13-integration
plan: 03
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - src/components/OutputPage.tsx
  - src/components/OutputViewer.tsx
  - .claude/skills/brand-compass/phase-1-origin/SKILL.md
  - .claude/skills/brand-compass/phase-2-audience/SKILL.md
  - .claude/skills/brand-compass/phase-3-positioning/SKILL.md
  - .claude/skills/brand-compass/phase-4-personality/SKILL.md
  - .claude/skills/brand-compass/phase-5-messaging/SKILL.md
  - .claude/skills/brand-compass/phase-6-voice/SKILL.md
  - .claude/skills/brand-compass/phase-7-visual/SKILL.md
  - .claude/skills/brand-compass/phase-8-toolkit/SKILL.md
  - .claude/skills/brand-compass/checkpoint-a/SKILL.md
  - .claude/skills/brand-compass/checkpoint-b/SKILL.md
autonomous: true
requirements:
  - INTG-05
  - INTG-06

must_haves:
  truths:
    - "React frontend displays functional skill-bundle and design-kit cards with file links when files exist"
    - "OutputViewer shows friendly titles for skill-bundle and design-kit files"
    - "The N of M files counter includes skill-bundle and design-kit files"
    - "Every phase skill file and checkpoint writes a Last Phase Handoff block to STATE.md on completion"
    - "Every phase completion message includes a /clear nudge"
    - "The placeholder cards with opacity-50 border-dashed are removed"
  artifacts:
    - path: "src/components/OutputPage.tsx"
      provides: "Functional bundle cards for all three bundles"
      contains: "Agent Skill Bundle"
    - path: "src/components/OutputViewer.tsx"
      provides: "Friendly file titles for new bundle files"
      contains: "Agent Skill Definition"
    - path: ".claude/skills/brand-compass/phase-1-origin/SKILL.md"
      provides: "Phase 1 Mark Complete with handoff + /clear nudge"
      contains: "Last Phase Handoff"
  key_links:
    - from: "OutputPage.tsx deliverables array"
      to: "workspace/output/skill-bundle/"
      via: "file existence fetch HEAD check"
      pattern: "skill-bundle/SKILL.md"
    - from: "OutputPage.tsx deliverables array"
      to: "workspace/output/design-kit/"
      via: "file existence fetch HEAD check"
      pattern: "design-kit/landing-page.html"
    - from: "phase skill Mark Complete steps"
      to: "workspace/STATE.md"
      via: "Last Phase Handoff section write"
      pattern: "Last Phase Handoff"
---

<objective>
Make the React frontend display functional skill-bundle and design-kit cards, and add phase transition handoff blocks to all phase skill files.

Purpose: The frontend currently has static placeholder cards for the two new bundles. They need to become functional with file existence checks and clickable links (matching the existing client deliverable pattern). Every phase skill file needs a standardized handoff block written to STATE.md at completion plus a /clear recommendation, so cross-session context is preserved.

Output: Updated OutputPage.tsx with functional bundle cards; updated OutputViewer.tsx with new file titles; 10 phase/checkpoint skill files updated with handoff block + /clear nudge.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-integration/13-CONTEXT.md
@.planning/phases/13-integration/13-RESEARCH.md
@.planning/phases/13-integration/13-PATTERNS.md
@.planning/phases/13-integration/13-01-SUMMARY.md

<interfaces>
<!-- Key structures from files being modified -->

From src/components/OutputPage.tsx:
- `deliverables` array (lines 10-47): array of { title, files: [{ filename, format }], description, icon }
- File existence check (lines 64-74): useEffect with fetch HEAD for each path in allPaths
- allCorePaths (line 54): deliverables.flatMap(d => d.files.map(f => f.filename))
- assetPackPaths (lines 55-57): from state.client.assetPacks
- allPaths (line 58): [...allCorePaths, ...assetPackPaths]
- Card render (lines 88-147): deliverables.map with exists/not-exists branching
- Placeholder cards (lines 150-175): Two static Card components with opacity-50 border-dashed — REMOVE these
- Import: FileText, Code from lucide-react (both already imported)

From src/components/OutputViewer.tsx:
- fileTitles map (lines 9-26): Record<string, string> mapping basename -> friendly title
- Path construction (line 37): srcUrl = `/workspace/output/${filePath}`
- Title resolution (line 36): `const title = fileTitles[filename] ?? filename`
- filename extraction (line 34): `const filename = filePath?.split('/').pop() ?? ''`
  Note: fileTitles key is the BASENAME (e.g., 'SKILL.md'), not the full path.
  This means 'SKILL.md' will match whether it's in skill-bundle/ or anywhere else.

From phase skill files — Mark Complete step pattern (see phase-1-origin/SKILL.md Step 6 lines 108-121):
  1. Update STATE.md: check phase complete, check discovery outputs, set Next Action, add Session Log entry
  2. Tell the client: "[completion message with next command]"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: React frontend — functional bundle cards and OutputViewer titles</name>
  <files>src/components/OutputPage.tsx, src/components/OutputViewer.tsx</files>
  <read_first>
    - src/components/OutputPage.tsx
    - src/components/OutputViewer.tsx
    - src/lib/content-loader.ts (verify import.meta.glob patterns already cover skill-bundle/ and design-kit/)
    - .planning/phases/13-integration/13-CONTEXT.md (D-15, D-16)
    - .planning/phases/13-integration/13-RESEARCH.md (Pattern 5: React Frontend — Functional Bundle Cards, Pitfall 2, Pitfall 3)
  </read_first>
  <action>
**OutputPage.tsx — 2 changes:**

**Change 1: Add skill-bundle and design-kit entries to the `deliverables` array (lines 10-47)**

Append these two entries after the existing UI Kit entry (line 47, before the closing `]`):

```typescript
{
  title: 'Agent Skill Bundle',
  files: [
    { filename: 'workspace/output/skill-bundle/SKILL.md', format: 'MD' },
    { filename: 'workspace/output/skill-bundle/brand-prompt.md', format: 'MD' },
    { filename: 'workspace/output/skill-bundle/source/voice-rules.md', format: 'MD' },
    { filename: 'workspace/output/skill-bundle/source/guardrails.md', format: 'MD' },
    { filename: 'workspace/output/skill-bundle/source/language-bank.md', format: 'MD' },
  ],
  description: 'Claude Code skill definition, brand prompt, and voice source files.',
  icon: FileText,
},
{
  title: 'Design Kit',
  files: [
    { filename: 'workspace/output/design-kit/landing-page.html', format: 'HTML' },
    { filename: 'workspace/output/design-kit/README.md', format: 'MD' },
    { filename: 'workspace/output/design-kit/HANDOFF.md', format: 'MD' },
  ],
  description: 'Atomized components, design tokens, preview cards, and reference specimens.',
  icon: Code,
},
```

The Design Kit card shows the 3 high-value navigable root files. The full 23-file set is too granular for cards — individual component/preview drill-down is future scope.

Because `allCorePaths` is `deliverables.flatMap(d => d.files.map(f => f.filename))`, the new files are automatically included in `allPaths` and the "N of M files" counter. No change needed to lines 54-61.

The `deliverables.map()` render at lines 88-147 already handles exists/not-exists card states. The new entries will render using the exact same card pattern with no render code changes.

**Change 2: Remove placeholder cards (lines 150-175)**

Delete the two static `<Card className="opacity-50 border-dashed">` blocks for "Agent Skill Bundle" and "Design Kit". These were placeholders — now replaced by functional entries in the deliverables array.

The `{/* Future Bundle Placeholders */}` comment and both Card blocks (lines 150-175) must be completely removed.

**OutputViewer.tsx — 1 change:**

**Add entries to the `fileTitles` map (lines 9-26)**

Append these entries to the fileTitles object, after the existing `'email-newsletter.html': 'Email & Newsletter'` entry:

```typescript
'SKILL.md': 'Agent Skill Definition',
'brand-prompt.md': 'Brand Prompt',
'voice-rules.md': 'Voice Rules',
'guardrails.md': 'Guardrails',
'language-bank.md': 'Language Bank',
'landing-page.html': 'Design Kit Landing Page',
'README.md': 'Design Kit README',
'HANDOFF.md': 'Design Kit Handoff Guide',
```

Note: fileTitles uses basenames as keys. The title lookup at line 36 (`fileTitles[filename]`) extracts the basename from the route path using `filePath?.split('/').pop()`. So `skill-bundle/SKILL.md` resolves to key `SKILL.md` and displays "Agent Skill Definition". This works correctly without full-path keys.
  </action>
  <verify>
    <automated>cd /Users/kelseyruger/Projects/a-emporium-working/gold-master-projects/brand-compass && npm run build</automated>
    Build must pass with zero TypeScript errors
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0 with no TypeScript errors
    - `grep -c "Agent Skill Bundle" src/components/OutputPage.tsx` returns at least 1 (deliverables entry)
    - `grep -c "Design Kit" src/components/OutputPage.tsx` returns at least 1 (deliverables entry)
    - `grep -c "skill-bundle/SKILL.md" src/components/OutputPage.tsx` returns 1 (file path in deliverables)
    - `grep -c "design-kit/landing-page.html" src/components/OutputPage.tsx` returns 1 (file path in deliverables)
    - `grep -c "opacity-50 border-dashed" src/components/OutputPage.tsx` returns 0 (placeholder cards removed)
    - `grep -c "Phase 11\|Phase 12" src/components/OutputPage.tsx` returns 0 (placeholder badges removed)
    - `grep -c "Agent Skill Definition" src/components/OutputViewer.tsx` returns 1 (fileTitles entry)
    - `grep -c "Brand Prompt" src/components/OutputViewer.tsx` returns 1
    - `grep -c "Design Kit Landing Page" src/components/OutputViewer.tsx` returns 1
    - `grep -c "Design Kit Handoff Guide" src/components/OutputViewer.tsx` returns 1
    - `grep -c "voice-rules.md" src/components/OutputViewer.tsx` returns 1
  </acceptance_criteria>
  <done>OutputPage.tsx has functional skill-bundle card (5 files) and design-kit card (3 representative files) in the deliverables array; placeholder cards removed; all new files included in the N-of-M counter via allCorePaths. OutputViewer.tsx fileTitles map has entries for all 8 new file basenames so the viewer shows friendly titles.</done>
</task>

<task type="auto">
  <name>Task 2: Phase skill files — handoff block and /clear nudge in all Mark Complete steps</name>
  <files>
    .claude/skills/brand-compass/phase-1-origin/SKILL.md,
    .claude/skills/brand-compass/phase-2-audience/SKILL.md,
    .claude/skills/brand-compass/phase-3-positioning/SKILL.md,
    .claude/skills/brand-compass/phase-4-personality/SKILL.md,
    .claude/skills/brand-compass/phase-5-messaging/SKILL.md,
    .claude/skills/brand-compass/phase-6-voice/SKILL.md,
    .claude/skills/brand-compass/phase-7-visual/SKILL.md,
    .claude/skills/brand-compass/phase-8-toolkit/SKILL.md,
    .claude/skills/brand-compass/checkpoint-a/SKILL.md,
    .claude/skills/brand-compass/checkpoint-b/SKILL.md
  </files>
  <read_first>
    - .claude/skills/brand-compass/phase-1-origin/SKILL.md (Step 6 — canonical Mark Complete pattern)
    - .claude/skills/brand-compass/phase-2-audience/SKILL.md (Step 6)
    - .claude/skills/brand-compass/phase-3-positioning/SKILL.md (Step 5)
    - .claude/skills/brand-compass/phase-4-personality/SKILL.md (Step 6)
    - .claude/skills/brand-compass/phase-5-messaging/SKILL.md (Step 7)
    - .claude/skills/brand-compass/phase-6-voice/SKILL.md (Step 7)
    - .claude/skills/brand-compass/phase-7-visual/SKILL.md (Step 7)
    - .claude/skills/brand-compass/phase-8-toolkit/SKILL.md (Step 7 — already modified in Plan 01)
    - .claude/skills/brand-compass/checkpoint-a/SKILL.md (Step 6)
    - .claude/skills/brand-compass/checkpoint-b/SKILL.md (Step 7)
    - .planning/phases/13-integration/13-CONTEXT.md (D-17, D-18, D-19, D-20)
    - .planning/phases/13-integration/13-PATTERNS.md (Phase Skill Files — Mark Complete Steps)
  </read_first>
  <action>
For each of the 10 files listed below, make two additions to the Mark Complete / Pass Checkpoint step. The additions are identical in structure but customized per-file for the phase name and next command.

**Addition 1: Handoff block write instruction (D-17, D-20)**

In the STATE.md update block (item 1 of the Mark Complete step), after "Add Session Log entry", add:

```
   - **Replace** the `## Last Phase Handoff` section in STATE.md with:
     ```
     ## Last Phase Handoff

     **Phase completed:** Phase N — [Phase Name]
     **Completed:** [today's date]

     **Key decisions made:**
     - [Key decisions from this phase — list 2-4 items]

     **Key outputs produced:**
     - [List output files or named discovery artifacts]

     **What happens next:**
     Run `[next command]` to continue.
     ```
     If `## Last Phase Handoff` does not yet exist, append it to the end of STATE.md.
```

**Addition 2: /clear nudge (D-18)**

Append to the existing "Tell the client" message (item 2) — add this sentence to the end of the message:

```
Suggest using `/clear` before starting [next phase] to begin with a fresh context window.
```

**Per-file customization:**

| File | Mark Complete Step | Phase Name | Next Command |
|------|-------------------|------------|--------------|
| phase-1-origin/SKILL.md | Step 6 (line 108) | Phase 1 — Origin & Belief | `/brand-compass:phase-2-audience` |
| phase-2-audience/SKILL.md | Step 6 (line 94) | Phase 2 — Audience | `/brand-compass:phase-3-positioning` |
| phase-3-positioning/SKILL.md | Step 5 (line 135) | Phase 3 — Positioning & Differentiation | `/brand-compass:checkpoint-a` |
| phase-4-personality/SKILL.md | Step 6 (line 98) | Phase 4 — Personality & Archetypes | `/brand-compass:phase-5-messaging` |
| phase-5-messaging/SKILL.md | Step 7 (line 103) | Phase 5 — Messaging | `/brand-compass:phase-6-voice` |
| phase-6-voice/SKILL.md | Step 7 (line 108) | Phase 6 — Voice | `/brand-compass:checkpoint-b` |
| phase-7-visual/SKILL.md | Step 7 (line 188) | Phase 7 — Visual Identity | `/brand-compass:phase-8-toolkit` |
| phase-8-toolkit/SKILL.md | Step 7 (line 197) | Phase 8 — Toolkit Assembly | `/brand-compass:export` |
| checkpoint-a/SKILL.md | Step 6 (line 93) | Checkpoint A | `/brand-compass:phase-4-personality` |
| checkpoint-b/SKILL.md | Step 7 (line 100) | Checkpoint B | `/brand-compass:phase-7-visual` |

**Important:** The instruction says "Replace" (not "Add" or "Append") the `## Last Phase Handoff` section, per D-20. Only one handoff block should ever exist in STATE.md.

**Important:** Phase 8 completion's "What happens next" should say `Run /brand-compass:export to continue.` and the /clear nudge should say "Suggest using /clear before running export to begin with a fresh context window."

**Important:** For checkpoint-a and checkpoint-b, the handoff block structure is the same but the "Phase completed" line should read "Checkpoint A — Strategic Foundation Review" and "Checkpoint B — Verbal Brand Review" respectively.
  </action>
  <verify>
    <automated>cd /Users/kelseyruger/Projects/a-emporium-working/gold-master-projects/brand-compass && grep -rl "Last Phase Handoff" .claude/skills/brand-compass/phase-*/SKILL.md .claude/skills/brand-compass/checkpoint-*/SKILL.md | wc -l</automated>
    Expected output: 10 (all 8 phase files + 2 checkpoint files)
  </verify>
  <acceptance_criteria>
    - `grep -rl "Last Phase Handoff" .claude/skills/brand-compass/phase-*/SKILL.md .claude/skills/brand-compass/checkpoint-*/SKILL.md | wc -l` returns 10
    - `grep -rl "/clear" .claude/skills/brand-compass/phase-*/SKILL.md .claude/skills/brand-compass/checkpoint-*/SKILL.md | wc -l` returns 10
    - `grep -c "Replace" .claude/skills/brand-compass/phase-1-origin/SKILL.md` returns at least 1 (overwrite instruction per D-20)
    - `grep -c "Last Phase Handoff" .claude/skills/brand-compass/phase-8-toolkit/SKILL.md` returns at least 1
    - `grep -c "/brand-compass:export" .claude/skills/brand-compass/phase-8-toolkit/SKILL.md` shows the next command in the handoff
    - `grep -c "Last Phase Handoff" .claude/skills/brand-compass/checkpoint-a/SKILL.md` returns at least 1
    - `grep -c "Last Phase Handoff" .claude/skills/brand-compass/checkpoint-b/SKILL.md` returns at least 1
    - `grep -c "does not yet exist" .claude/skills/brand-compass/phase-1-origin/SKILL.md` returns at least 1 (first-time creation instruction)
    - None of the 10 files contain "Add a handoff block" or "Append a handoff" (must say "Replace")
  </acceptance_criteria>
  <done>All 10 phase/checkpoint skill files have a "Replace the ## Last Phase Handoff section" instruction in their Mark Complete step with per-phase customization (phase name, key decisions template, next command), plus a /clear nudge appended to the completion message. The handoff block is overwritten (not appended) per D-20.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser fetch -> Vite dev server | OutputPage.tsx makes HEAD requests to check file existence; the Vite dev server serves files from workspace/ |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-03 | I (Information Disclosure) | OutputPage.tsx | accept | File existence checks expose which deliverables exist. This is a local-only development dashboard — no external access. |
| T-13-04 | T (Tampering) | Phase skill files | accept | Handoff block is written by the agent to STATE.md. The agent already has full write access to workspace/ — no new attack surface. |
</threat_model>

<verification>
After both tasks complete:
1. `npm run build` passes (TypeScript and Vite build)
2. `npm run lint` passes
3. `grep -c "opacity-50 border-dashed" src/components/OutputPage.tsx` returns 0
4. `grep -rl "Last Phase Handoff" .claude/skills/brand-compass/phase-*/SKILL.md .claude/skills/brand-compass/checkpoint-*/SKILL.md | wc -l` returns 10
5. `grep -rl "/clear" .claude/skills/brand-compass/phase-*/SKILL.md .claude/skills/brand-compass/checkpoint-*/SKILL.md | wc -l` returns 10
</verification>

<success_criteria>
React frontend displays functional skill-bundle and design-kit cards with file links and existence checks. OutputViewer shows friendly titles for new bundle files. The N-of-M counter includes all bundle files. Every phase and checkpoint writes a structured Last Phase Handoff block to STATE.md at completion and recommends /clear before the next phase.
</success_criteria>

<output>
After completion, create `.planning/phases/13-integration/13-03-SUMMARY.md`
</output>
