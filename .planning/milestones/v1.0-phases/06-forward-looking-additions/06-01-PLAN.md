---
phase: 06-forward-looking-additions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
  - .claude/agents/visual-director.md
  - .claude/agents/image-generator.md
  - .claude/agents/document-assembler.md
autonomous: true
requirements:
  - FUTR-02

must_haves:
  truths:
    - "Phase 7 discovery includes 2-3 graphic device questions positioned after Imagery and before Motion"
    - "Visual Director output template includes a Pattern Direction section specifying style, motifs, and color usage rules"
    - "Image Generator is directed to produce 1-3 brand patterns from the Visual Director's pattern direction"
    - "Document Assembler visual-system.html spec includes a Graphic Devices section for rendering pattern specimens"
    - "Graphic device discovery is always asked but output is optional — Visual Director can note 'no graphic devices recommended'"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Graphic device discovery questions in Phase 7, updated launch prompt, updated phase complete criteria"
      contains: "Graphic Devices"
    - path: ".claude/agents/visual-director.md"
      provides: "Pattern Direction output section in visual-direction.md template"
      contains: "Pattern Direction"
    - path: ".claude/agents/image-generator.md"
      provides: "Brand pattern generation directive"
      contains: "brand pattern"
    - path: ".claude/agents/document-assembler.md"
      provides: "Graphic Devices section spec for visual-system.html"
      contains: "Graphic Devices"
  key_links:
    - from: "CLAUDE.md Phase 7 graphic device discovery"
      to: ".claude/agents/visual-director.md Pattern Direction"
      via: "Discovery answers feed Visual Director input"
      pattern: "Pattern Direction"
    - from: ".claude/agents/visual-director.md Pattern Direction"
      to: ".claude/agents/image-generator.md"
      via: "Visual Director output feeds Image Generator"
      pattern: "brand pattern"
    - from: ".claude/agents/visual-director.md Pattern Direction"
      to: ".claude/agents/document-assembler.md visual-system.html"
      via: "Pattern Direction rendered in HTML specimen"
      pattern: "Graphic Devices"
---

<objective>
Add graphic device direction to the Phase 7 visual identity pipeline — discovery questions, Visual Director pattern output, Image Generator pattern production, and Document Assembler HTML rendering.

Purpose: Graphic devices (patterns, textures, decorative elements) give brands visual texture beyond color, type, and mark. Many clients don't know they need them until prompted, so discovery is always asked but output is conditional on brand fit.
Output: Updated CLAUDE.md Phase 7, visual-director.md, image-generator.md, document-assembler.md
</objective>

<execution_context>
@/Users/kelseyruger/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kelseyruger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-forward-looking-additions/06-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add graphic device discovery and update Phase 7 flow in CLAUDE.md</name>
  <files>CLAUDE.md</files>
  <action>
In CLAUDE.md Phase 7: Visual Identity section, add a "Graphic Devices" discovery subsection positioned AFTER Imagery (Q20-23) and BEFORE Motion (Q26-28). This matches the sequence specified in CONTEXT.md: Color -> Typography -> Mark -> Imagery -> Graphic Devices -> Motion -> Core+Flex -> Application.

Since the current Application section is Q24-25 and Motion is Q26-28, the new questions need to shift the numbering. Insert Graphic Devices as Q24-26 (3 questions), then renumber Application to Q27-28, Motion to Q29-31, Core+Flex to Q32-33, AI-Generation Rules to Q34-35.

Wait — re-reading the CONTEXT.md sequence: "Color -> typography -> mark -> imagery -> graphic devices -> motion -> core+flex -> application." Currently the order is: Color, Typography, Mark/Logo, Imagery, Application, Motion, Core+Flex, AI-Generation Rules. The CONTEXT.md moves Application to the end. So the new order should be: Color -> Typography -> Mark/Logo -> Imagery -> Graphic Devices -> Motion -> Core+Flex -> AI-Generation Rules -> Application. Move the existing Application section (Q24-25) to AFTER AI-Generation Rules.

Add this new section after Imagery (after Q23):

```
Graphic Devices:
24. Where does your brand need visual texture — backgrounds, section dividers, social media frames, presentation slides, packaging? What surfaces feel too plain right now?
25. When you imagine those surfaces with pattern or texture, what feeling should it carry? Should it reinforce the brand's energy or provide a quiet backdrop?
26. Are there any recurring visual motifs in your work or world — grids, organic shapes, hand-drawn marks, geometric repetition — that feel like they belong to your brand?
```

Follow anti-anchoring principles from Phase 5: use-case framing first (WHERE do you need texture), then feeling, then motifs. No style anchoring (don't suggest "geometric or organic" in the first question).

Add a facilitator note after the questions:
```
*(Not every brand needs graphic devices. If the client's brand world is minimal and the discovery answers don't surface a need, the Visual Director can note "no graphic devices recommended" and skip pattern generation. Always ask — some clients discover a need they didn't know they had.)*
```

Renumber all subsequent questions accordingly (Motion becomes Q27-29, Core+Flex Q30-31, AI-Generation Rules Q32-33, Application moves to Q34-35).

Update the **Launch prompt** to mention graphic devices:
Change "into visual direction including motion principles" to "into visual direction including motion principles and graphic device direction (if applicable)"

Update the **Phase complete when** line to add "graphic devices specified (if applicable)" to the list.
  </action>
  <verify>
    <automated>grep -n "Graphic Devices" CLAUDE.md | head -5 && grep -c "graphic device" CLAUDE.md</automated>
  </verify>
  <done>Phase 7 section contains 2-3 graphic device discovery questions positioned after Imagery and before Motion, with use-case framing and facilitator note about optional output. All subsequent questions renumbered. Launch prompt and phase-complete criteria updated.</done>
</task>

<task type="auto">
  <name>Task 2: Add Pattern Direction to Visual Director, pattern production to Image Generator, and graphic devices rendering to Document Assembler</name>
  <files>.claude/agents/visual-director.md, .claude/agents/image-generator.md, .claude/agents/document-assembler.md</files>
  <action>
**Visual Director (.claude/agents/visual-director.md):**

1. In the "## Your Task" numbered list, add step 10: "Define graphic device direction — pattern style, motifs, color usage, and application guidance (if brand calls for it). If the brand world is minimal and doesn't warrant graphic devices, note 'No graphic devices recommended for this brand' and skip."

2. In the "## Output Format" template (the markdown code block), add a new section AFTER "## AI Image Generation Prompt" and BEFORE "## Motion Direction":

```markdown
## Pattern Direction

*(Include this section if graphic devices are appropriate for this brand. If not, write: "No graphic devices recommended — [brief rationale, e.g., 'the brand's minimal aesthetic relies on white space and typography contrast, not decorative elements'].")*

### Style
[Geometric, organic, textural, illustrative, or hybrid — and WHY this style for this brand. Ground it in the brand world: "The repeating angular forms echo the precision of the brand's workshop metaphor."]

### Suggested Motifs
[2-3 specific motif concepts tied to the brand world, personality, or core metaphor. Not generic descriptions — specific enough that the Image Generator can execute.]
- **[Motif 1]:** [Description and connection to brand]
- **[Motif 2]:** [Description and connection to brand]

### Color Usage
[How patterns use the palette — monochromatic using a single tint scale? Multi-color from accent palette? Tone-on-tone with primary colors? What opacity/density?]

### Application Guidance
[Where these patterns should appear (backgrounds, dividers, social frames, presentation slides) and at what density. Include scaling notes — a pattern used as a full-bleed background needs different density than one used as a thin divider strip.]

### What to Avoid
[Pattern styles that would be wrong for this brand — e.g., "No paisley or floral motifs — too decorative for the brand's utilitarian personality."]
```

3. In the "## Quality Bar" section, add a bullet: "- **Pattern direction grounded in brand world:** If graphic devices are included, motifs should trace back to the brand's physical metaphor or personality traits — not decorative filler. If devices aren't included, the rationale should explain why."

**Image Generator (.claude/agents/image-generator.md):**

1. In the "## Your Task" section, after the existing "### For Illustration Samples" block, add:

```markdown
### For Brand Patterns
- Read the Pattern Direction section from `workspace/drafts/visual-direction.md`
- Generate 1-3 seamless/tileable patterns based on the suggested motifs
- Each pattern should work at multiple scales (full background, divider strip, accent element)
- Use the palette constraints from Pattern Direction's color usage guidance
- Include one pattern that's subtle enough for backgrounds and one that's bold enough for feature use
- If Pattern Direction says "No graphic devices recommended," skip pattern generation entirely
```

2. In the "## Output Location" section, add: `- `workspace/assets/brand-patterns/``

3. In the output format markdown template, after the existing example, add a note: "For brand patterns, also include: scale recommendations (what size works for backgrounds vs. dividers), tile dimensions, and color variant notes."

**Document Assembler (.claude/agents/document-assembler.md):**

1. In the "### Document 6: `workspace/output/visual-system.html`" section, add a new subsection after "**Do/Don't Examples for Imagery**":

```markdown
**Graphic Devices** (new section, after motion direction):
- Read the Pattern Direction section from `workspace/drafts/visual-direction.md`
- If Pattern Direction says "No graphic devices recommended," omit this section entirely
- If patterns exist, render as a dedicated section with:
  - Pattern style description as a rationale card (same style as visual principles cards)
  - Pattern specimens: display each generated pattern from `workspace/assets/brand-patterns/` as image cards with name, description, and recommended applications
  - Application guidance: a grid showing pattern at different scales/contexts (background, divider, accent)
  - Color usage rules: which palette colors the patterns use and why
  - Do/don't examples using the `.do-dont-grid` pattern: correct pattern usage (subtle background, divider accent) vs incorrect (overwhelming density, wrong color variant, competing with content)
```

2. In the "**visual-system.html:**" subsection of "### Personality Application Notes", add: "- Graphic device cards: follow the 'card style' parameter. Pattern specimens displayed at actual rendering size where practical."
  </action>
  <verify>
    <automated>grep -n "Pattern Direction" .claude/agents/visual-director.md | head -3 && grep -n "Brand Patterns" .claude/agents/image-generator.md | head -3 && grep -n "Graphic Devices" .claude/agents/document-assembler.md | head -3</automated>
  </verify>
  <done>Visual Director includes Pattern Direction output section with style/motifs/color/application/avoid structure. Image Generator includes brand pattern production directive for 1-3 patterns. Document Assembler includes graphic devices section spec for visual-system.html with pattern specimens, application guidance, and do/don't examples. All three agents handle the "no graphic devices" case gracefully.</done>
</task>

</tasks>

<verification>
1. CLAUDE.md Phase 7 contains "Graphic Devices:" section with 2-3 questions after Imagery, before Motion
2. Question numbering is consistent throughout Phase 7 (no gaps, no duplicates)
3. Visual Director output template includes "## Pattern Direction" section
4. Image Generator "## Your Task" includes "### For Brand Patterns" subsection
5. Document Assembler visual-system.html spec includes "**Graphic Devices**" subsection
6. All three agents handle the "no graphic devices" case (Visual Director notes rationale, Image Generator skips, Document Assembler omits section)
7. Phase 7 launch prompt mentions graphic devices
8. Phase 7 "Phase complete when" mentions graphic devices
</verification>

<success_criteria>
- Graphic device pipeline follows the established pattern: discovery questions -> Visual Director specification -> Image Generator execution -> Document Assembler rendering
- Discovery questions use use-case framing (WHERE do you need texture) before style framing, following anti-anchoring principles
- Output is conditional: always asked, optionally produced based on brand fit
- All question numbers in Phase 7 are sequential with no gaps after insertion and reordering
</success_criteria>

<output>
After completion, create `.planning/phases/06-forward-looking-additions/06-01-SUMMARY.md`
</output>
