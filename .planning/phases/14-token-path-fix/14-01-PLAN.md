---
phase: 14-token-path-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/agents/design-kit-foundation.md
  - .claude/agents/design-kit-packager.md
autonomous: true
requirements:
  - FOUN-02
  - DKIT-05

must_haves:
  truths:
    - "Root-level design-kit HTML instructions use href=\"tokens/...\" (sibling path)"
    - "Subdirectory HTML instructions still use href=\"../tokens/...\" (parent path) unchanged"
    - "Quality Bar in design-kit-foundation.md asserts tokens/ not ../tokens/ for root-level files"
    - "Prose rationale in both agent files correctly explains sibling relationship"
  artifacts:
    - path: ".claude/agents/design-kit-foundation.md"
      provides: "Corrected Task 2 link example + corrected Quality Bar assertion"
      contains: 'href="tokens/colors.css"'
    - path: ".claude/agents/design-kit-packager.md"
      provides: "Corrected Task 3 landing-page link example"
      contains: 'href="tokens/colors.css"'
  key_links:
    - from: ".claude/agents/design-kit-foundation.md Task 2"
      to: "workspace/output/design-kit/*.html (root-level specimens)"
      via: "Agent reads Task 2 link example and writes it into HTML output"
      pattern: 'href="tokens/'
    - from: ".claude/agents/design-kit-packager.md Task 3"
      to: "workspace/output/design-kit/landing-page.html"
      via: "Agent reads Task 3 link example and writes it into HTML output"
      pattern: 'href="tokens/'
---

<objective>
Fix token CSS link paths in two agent definition files so root-level design-kit HTML files resolve token stylesheets correctly.

Purpose: Root-level design-kit specimens (brand-foundation.html, color-palette.html, landing-page.html) currently use `href="../tokens/..."` which resolves to the non-existent `workspace/output/tokens/`. The correct sibling path is `href="tokens/..."`. This is a definition-only fix — the agents have not yet produced output, so correcting the instructions prevents broken output from ever being written.

Output: Two corrected agent definition files with accurate path strings, corrected prose rationale, and corrected Quality Bar assertion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-token-path-fix/14-RESEARCH.md
@.planning/phases/14-token-path-fix/14-PATTERNS.md

<interfaces>
<!-- Both files are markdown agent definitions. No code interfaces — the "interface" is the
     HTML link tag pattern that agents copy into their output. -->

Correct root-level pattern (sibling path — tokens/ is next to the HTML file):
```html
<link rel="stylesheet" href="tokens/colors.css">
<link rel="stylesheet" href="tokens/typography.css">
<link rel="stylesheet" href="tokens/spacing.css">
```

Correct subdirectory pattern (parent path — must go up one level first):
```html
<link rel="stylesheet" href="../tokens/colors.css">
<link rel="stylesheet" href="../tokens/typography.css">
<link rel="stylesheet" href="../tokens/spacing.css">
```

Already-correct instance confirming root-level pattern:
- design-kit-packager.md line 349: `href="tokens/colors.css"` (README Developer Quick Start)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix design-kit-foundation.md — Task 2 link example and Quality Bar</name>
  <files>.claude/agents/design-kit-foundation.md</files>

  <read_first>
    - .claude/agents/design-kit-foundation.md (full file — executor must see current buggy lines at 250-254 and 282, and verify no other root-level path references exist)
    - .planning/phases/14-token-path-fix/14-RESEARCH.md (Precise Change Specification section — Change A and Change B)
    - .planning/phases/14-token-path-fix/14-PATTERNS.md (Lines That Must NOT Change section)
  </read_first>

  <action>
Make three edits to `.claude/agents/design-kit-foundation.md`:

**Edit 1 — Task 2 step d link tags (lines 250-252):**
Change:
```
      <link rel="stylesheet" href="../tokens/colors.css">
      <link rel="stylesheet" href="../tokens/typography.css">
      <link rel="stylesheet" href="../tokens/spacing.css">
```
To:
```
      <link rel="stylesheet" href="tokens/colors.css">
      <link rel="stylesheet" href="tokens/typography.css">
      <link rel="stylesheet" href="tokens/spacing.css">
```

**Edit 2 — Task 2 step d prose rationale (line 254):**
Change:
```
      The relative path `../tokens/` is correct because design-kit HTML files sit at `workspace/output/design-kit/[filename].html` -- one level up from the tokens directory at `workspace/output/design-kit/tokens/`.
```
To:
```
      The relative path `tokens/` is correct because design-kit HTML files sit at `workspace/output/design-kit/[filename].html` -- at the same level as the `tokens/` directory, which is a sibling (not a parent). Using `../tokens/` would resolve to `workspace/output/tokens/` which does not exist.
```

**Edit 3 — Quality Bar assertion (line 282):**
Change:
```
- External `<link>` tags use relative paths (`../tokens/`) -- correct because design-kit HTML files sit at `workspace/output/design-kit/` alongside the `tokens/` directory
```
To:
```
- External `<link>` tags use relative paths (`tokens/`) -- correct because design-kit HTML files sit at `workspace/output/design-kit/` alongside the `tokens/` sibling directory (NOT `../tokens/` which would resolve to the non-existent `workspace/output/tokens/`)
```

**Do NOT change** any other lines. Specifically, do not touch anything in sections about subdirectory files (components/, previews/) — those correctly use `../tokens/`.
  </action>

  <verify>
    <automated>cd /Users/kelseyruger/Projects/a-emporium-working/gold-master-projects/brand-compass && grep -n 'href="tokens/' .claude/agents/design-kit-foundation.md && echo "---" && grep -n 'href="../tokens/' .claude/agents/design-kit-foundation.md && echo "---" && grep -n 'relative paths.*tokens/' .claude/agents/design-kit-foundation.md</automated>
  </verify>

  <acceptance_criteria>
    - Line 250 contains `href="tokens/colors.css"` (no `../` prefix)
    - Line 251 contains `href="tokens/typography.css"` (no `../` prefix)
    - Line 252 contains `href="tokens/spacing.css"` (no `../` prefix)
    - Line 254 contains the string `The relative path \`tokens/\` is correct`
    - Line 254 contains the string `Using \`../tokens/\` would resolve to`
    - Line 282 contains `relative paths (\`tokens/\`)` (no `../` prefix)
    - Line 282 contains `NOT \`../tokens/\``
    - `grep -c 'href="../tokens/' .claude/agents/design-kit-foundation.md` returns 0 (no remaining `../tokens/` href references in this file — this file only has root-level HTML instructions)
  </acceptance_criteria>

  <done>design-kit-foundation.md Task 2 link example uses tokens/ (sibling path), prose rationale correctly explains sibling relationship, and Quality Bar assertion matches corrected path.</done>
</task>

<task type="auto">
  <name>Task 2: Fix design-kit-packager.md — Task 3 landing-page link example</name>
  <files>.claude/agents/design-kit-packager.md</files>

  <read_first>
    - .claude/agents/design-kit-packager.md (full file — executor must see current buggy lines at 389-397, and confirm lines 63-65, 84, 197, and 349 are correct and must NOT change)
    - .planning/phases/14-token-path-fix/14-RESEARCH.md (Precise Change Specification section — Change C)
    - .planning/phases/14-token-path-fix/14-PATTERNS.md (Lines That Must NOT Change section — lists 4 locations that must be preserved)
  </read_first>

  <action>
Make one edit to `.claude/agents/design-kit-packager.md` at lines 389-397:

**Edit — Task 3 landing-page link block (lines 389-397):**
Change:
```
**Token link pattern — ONE level up because landing-page.html is at design-kit root:**

```html
<link rel="stylesheet" href="../tokens/colors.css">
<link rel="stylesheet" href="../tokens/typography.css">
<link rel="stylesheet" href="../tokens/spacing.css">
```

Do NOT use `../../tokens/` for this file. It sits at `workspace/output/design-kit/landing-page.html`, one level above the `tokens/` directory — not two levels.
```

To:
```
**Token link pattern — sibling directory because landing-page.html is at design-kit root:**

```html
<link rel="stylesheet" href="tokens/colors.css">
<link rel="stylesheet" href="tokens/typography.css">
<link rel="stylesheet" href="tokens/spacing.css">
```

Do NOT use `../tokens/` for this file. It sits at `workspace/output/design-kit/landing-page.html` — at the same level as `tokens/`, which is a sibling directory. `../tokens/` would resolve to the non-existent `workspace/output/tokens/`.
```

**Do NOT change** any of these lines (they are correct for subdirectory files):
- Lines 63-65: Task 1 component HTML template using `../tokens/`
- Line 84: CRITICAL path rule comment for components/
- Line 197: CRITICAL path rule for previews/
- Line 349: README Developer Quick Start example already using `tokens/` (correct)
  </action>

  <verify>
    <automated>cd /Users/kelseyruger/Projects/a-emporium-working/gold-master-projects/brand-compass && echo "=== Root-level (Task 3) should use tokens/ ===" && grep -n 'href="tokens/' .claude/agents/design-kit-packager.md && echo "=== Subdirectory (Task 1, previews) should still use ../tokens/ ===" && grep -n 'href="../tokens/' .claude/agents/design-kit-packager.md && echo "=== Line 389 heading ===" && sed -n '389p' .claude/agents/design-kit-packager.md</automated>
  </verify>

  <acceptance_criteria>
    - Line 389 contains `sibling directory because landing-page.html is at design-kit root` (not "ONE level up")
    - Line 391 contains `href="tokens/colors.css"` (no `../` prefix)
    - Line 392 contains `href="tokens/typography.css"` (no `../` prefix)
    - Line 393 contains `href="tokens/spacing.css"` (no `../` prefix)
    - Line 397 contains `Do NOT use \`../tokens/\` for this file`
    - Line 397 contains `which is a sibling directory`
    - Lines 63-65 still contain `href="../tokens/` (subdirectory pattern preserved)
    - Line 84 still contains the CRITICAL path rule for components/
    - Line 197 still contains the CRITICAL path rule for previews/
    - Line 349 still contains `href="tokens/colors.css"` (already correct, unchanged)
    - `grep -c 'href="../tokens/' .claude/agents/design-kit-packager.md` returns exactly 3 (the three correct subdirectory references at lines 63-65, NOT any root-level references)
  </acceptance_criteria>

  <done>design-kit-packager.md Task 3 landing-page link example uses tokens/ (sibling path), prose rationale correctly explains sibling relationship, and all subdirectory ../tokens/ references are preserved unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries apply. This phase edits agent instruction text (markdown files) with no authentication, user input handling, or network activity.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | Agent definition files | accept | Files are local markdown in a git repo; tampering requires local filesystem access which is already trusted |
</threat_model>

<verification>
Run these commands after both tasks complete:

```bash
# 1. Confirm no ../tokens/ href remains in root-level contexts of design-kit-foundation.md
grep -c 'href="../tokens/' .claude/agents/design-kit-foundation.md
# Expected: 0

# 2. Confirm exactly 3 ../tokens/ href remain in design-kit-packager.md (subdirectory contexts only)
grep -c 'href="../tokens/' .claude/agents/design-kit-packager.md
# Expected: 3

# 3. Confirm root-level pattern present in both files
grep 'href="tokens/colors.css"' .claude/agents/design-kit-foundation.md .claude/agents/design-kit-packager.md
# Expected: matches in both files

# 4. Confirm Quality Bar corrected
grep 'relative paths.*tokens/' .claude/agents/design-kit-foundation.md
# Expected: line with (`tokens/`) and NOT `../tokens/`
```
</verification>

<success_criteria>
1. Root-level design-kit HTML instructions in both agent files use `href="tokens/..."` (sibling path)
2. Subdirectory HTML instructions in design-kit-packager.md still use `href="../tokens/..."` (parent path) — exactly 3 occurrences
3. Quality Bar assertion in design-kit-foundation.md references `tokens/` not `../tokens/`
4. Prose rationale in both files correctly explains that tokens/ is a sibling directory of root-level HTML files
5. Zero occurrences of `href="../tokens/"` in design-kit-foundation.md (this file only governs root-level specimens)
</success_criteria>

<output>
After completion, create `.planning/phases/14-token-path-fix/14-01-SUMMARY.md`
</output>
