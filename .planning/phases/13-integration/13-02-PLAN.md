---
phase: 13-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/agents/brand-verifier.md
  - CLAUDE.md
autonomous: true
requirements:
  - INTG-02
  - INTG-03

must_haves:
  truths:
    - "Brand Verifier has Level 7 (Skill Bundle) and Level 8 (Design Kit) checks"
    - "Verifier Level 7 and Level 8 are conditional on Phase 8 completion — absent bundles are info messages, not failures"
    - "CLAUDE.md Agent Table includes rows for skill-bundle-packager and design-kit-packager"
    - "CLAUDE.md has a Workspace Structure section showing the three-bundle output directory"
    - "CLAUDE.md has a Multi-Bundle Output section describing what each bundle contains"
  artifacts:
    - path: ".claude/agents/brand-verifier.md"
      provides: "Verification levels for all three output bundles"
      contains: "Level 7: Skill Bundle"
    - path: "CLAUDE.md"
      provides: "System documentation with three-bundle model"
      contains: "Workspace Structure"
  key_links:
    - from: "brand-verifier.md Level 7"
      to: "workspace/output/skill-bundle/"
      via: "file existence and substance checks"
      pattern: "skill-bundle/SKILL.md"
    - from: "brand-verifier.md Level 8"
      to: "workspace/output/design-kit/"
      via: "file existence and substance checks"
      pattern: "design-kit/components"
    - from: "CLAUDE.md Agent Table"
      to: ".claude/agents/skill-bundle-packager.md"
      via: "agent row reference"
      pattern: "skill-bundle-packager"
---

<objective>
Extend the Brand Verifier with skill-bundle and design-kit checks, and update CLAUDE.md to document the three-bundle model.

Purpose: The verifier currently only validates client output (Levels 1-6). It needs Level 7 (skill-bundle) and Level 8 (design-kit) to provide a comprehensive quality gate. CLAUDE.md needs to document the new agents and the three-bundle workspace structure so the system is self-describing.

Output: Updated brand-verifier.md with Levels 7-8; updated CLAUDE.md with new agent rows, Workspace Structure section, and Multi-Bundle Output section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-integration/13-CONTEXT.md
@.planning/phases/13-integration/13-RESEARCH.md
@.planning/phases/13-integration/13-PATTERNS.md

<interfaces>
<!-- Key structures from files being modified -->

From .claude/agents/brand-verifier.md:
- Levels 1-6 structure (lines 30-226): Each level has a heading, condition, and checks
- Level 6 conditional skip pattern (line 197): "Skip this level if the voice fingerprint doesn't exist"
- Output Format Scores table (lines 240-247): rows for Existence, Substance, Consistency, Coverage, Personality, Voice Compliance
- Output Format Issues section: Critical, Warning, Info categories

From CLAUDE.md:
- Agent Table (lines 144-157): 9 rows from Research Analyst through Brand Verifier
- Section separator "---" at line 158
- Background Agent Protocol starts at line 160
- No existing Workspace Structure or Multi-Bundle Output sections
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Brand Verifier — add Level 7 (Skill Bundle) and Level 8 (Design Kit)</name>
  <files>.claude/agents/brand-verifier.md</files>
  <read_first>
    - .claude/agents/brand-verifier.md
    - .claude/agents/skill-bundle-packager.md (Quality Bar section — exact checks to mirror)
    - .claude/agents/design-kit-packager.md (Quality Bar section — exact checks to mirror)
    - .claude/agents/design-kit-foundation.md (Output section — token file list)
    - .planning/phases/13-integration/13-CONTEXT.md (D-04, D-05, D-06, D-07)
  </read_first>
  <action>
Three additions to brand-verifier.md:

**Addition 1: Level 7 — Skill Bundle (after Level 6, before Output Format)**

Insert after the Level 6 section (after line ~225) and before the `## Output Format` section:

```markdown
### Level 7: Skill Bundle

Only run if Phase 8 is complete. If `workspace/output/skill-bundle/` is entirely absent, report:
"Skill Bundle: Not yet generated — run skill-bundle-packager after Phase 8 assembly completes."
Skip to Level 8.

**Level 7A: Existence**

Check that all 5 expected files exist:
- `workspace/output/skill-bundle/SKILL.md`
- `workspace/output/skill-bundle/brand-prompt.md`
- `workspace/output/skill-bundle/source/voice-rules.md`
- `workspace/output/skill-bundle/source/guardrails.md`
- `workspace/output/skill-bundle/source/language-bank.md`

**Level 7B: Substance**

- Read `workspace/output/skill-bundle/SKILL.md`: count lines. Flag as Critical if under 200.
- Read `workspace/output/skill-bundle/brand-prompt.md`: scan for any `#` character. Flag as Critical if found. Count words (split on whitespace). Flag as Critical if under 150 or over 300.
- Read each source/ file: flag as Critical if any file is empty (0 bytes or whitespace only).
```

**Addition 2: Level 8 — Design Kit (after Level 7)**

```markdown
### Level 8: Design Kit

Only run if Phase 8 is complete. If `workspace/output/design-kit/` is entirely absent, report:
"Design Kit: Not yet generated — run design-kit-foundation then design-kit-packager after Phase 8 assembly completes."
Skip.

**Level 8A: Existence** (23 files total)

Token files (5):
- `workspace/output/design-kit/tokens/colors.css`
- `workspace/output/design-kit/tokens/typography.css`
- `workspace/output/design-kit/tokens/spacing.css`
- `workspace/output/design-kit/tokens/tokens.json`
- `workspace/output/design-kit/tokens/tailwind.config.js`

Components (7):
- `workspace/output/design-kit/components/button.html`
- `workspace/output/design-kit/components/card.html`
- `workspace/output/design-kit/components/form-field.html`
- `workspace/output/design-kit/components/nav.html`
- `workspace/output/design-kit/components/modal.html`
- `workspace/output/design-kit/components/alert.html`
- `workspace/output/design-kit/components/badge.html`

Previews (5):
- `workspace/output/design-kit/previews/colors.html`
- `workspace/output/design-kit/previews/type.html`
- `workspace/output/design-kit/previews/spacing.html`
- `workspace/output/design-kit/previews/components.html`
- `workspace/output/design-kit/previews/brand-groups.html`

Root files (4):
- `workspace/output/design-kit/README.md`
- `workspace/output/design-kit/package.json`
- `workspace/output/design-kit/HANDOFF.md`
- `workspace/output/design-kit/landing-page.html`

Post-processed specimens (2):
- `workspace/output/design-kit/brand-foundation.html`
- `workspace/output/design-kit/color-palette.html`

**Level 8B: Substance**

- Scan each `workspace/output/design-kit/components/*.html` file for `:root {` blocks. Flag as Critical if found — components must link external tokens, not define their own.
- Read `workspace/output/design-kit/landing-page.html`: scan for `[` characters that indicate `[bracket]` placeholder markers. Flag as Critical if any are present.
- Read any `workspace/output/design-kit/previews/*.html` file: check that `.preview-card` style includes `min-width: 150px`. Flag as Warning if absent.
```

**Addition 3: Update the Output Format Scores table**

Find the Scores table in the Output Format section (the `| Check | Score | Details |` table). Add two new rows after the existing Voice Compliance row:

```markdown
| Skill Bundle | N/5 files | [missing files if any] |
| Design Kit   | N/23 files | [missing files if any] |
```

Also update the Input section at the top of the file (after line 24) to add:
```markdown
- All files in `workspace/output/skill-bundle/` directory
- All files in `workspace/output/design-kit/` directory
```
  </action>
  <verify>
    <automated>cd /Users/kelseyruger/Projects/a-emporium-working/gold-master-projects/brand-compass && grep -c "Level 7\|Level 8" .claude/agents/brand-verifier.md</automated>
    Expected output: at least 6 (Level 7, Level 7A, Level 7B, Level 8, Level 8A, Level 8B)
  </verify>
  <acceptance_criteria>
    - `grep -c "Level 7: Skill Bundle" .claude/agents/brand-verifier.md` returns 1
    - `grep -c "Level 7A: Existence" .claude/agents/brand-verifier.md` returns 1
    - `grep -c "Level 7B: Substance" .claude/agents/brand-verifier.md` returns 1
    - `grep -c "Level 8: Design Kit" .claude/agents/brand-verifier.md` returns 1
    - `grep -c "Level 8A: Existence" .claude/agents/brand-verifier.md` returns 1
    - `grep -c "Level 8B: Substance" .claude/agents/brand-verifier.md` returns 1
    - `grep -c "Not yet generated" .claude/agents/brand-verifier.md` returns at least 2 (one per new level conditional skip)
    - `grep -c "N/5 files" .claude/agents/brand-verifier.md` returns at least 1 (Scores table row)
    - `grep -c "N/23 files" .claude/agents/brand-verifier.md` returns at least 1 (Scores table row)
    - `grep -c "skill-bundle/" .claude/agents/brand-verifier.md` returns at least 6 (5 file paths + Input section)
    - `grep -c ":root {" .claude/agents/brand-verifier.md` returns at least 1 (substance check for inline tokens)
    - `grep -c "min-width: 150px" .claude/agents/brand-verifier.md` returns 1 (preview card width check)
    - `grep "workspace/output/skill-bundle/" .claude/agents/brand-verifier.md` shows all 5 skill-bundle file paths
    - `grep "workspace/output/design-kit/components/" .claude/agents/brand-verifier.md` shows all 7 component file paths
  </acceptance_criteria>
  <done>Brand Verifier has Level 7 (Skill Bundle) with 5-file existence check + substance checks (line count, heading scan, word count, non-empty), Level 8 (Design Kit) with 23-file existence check + substance checks (no inline :root blocks, no bracket placeholders, preview min-width), both conditional on Phase 8 completion, and Scores table updated with two new rows.</done>
</task>

<task type="auto">
  <name>Task 2: CLAUDE.md — agent table rows, Workspace Structure, Multi-Bundle Output</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md
    - .claude/agents/skill-bundle-packager.md (frontmatter — trigger, input, output for agent table row)
    - .claude/agents/design-kit-packager.md (frontmatter — trigger, input, output for agent table row)
    - .claude/agents/design-kit-foundation.md (frontmatter — trigger, input, output for agent table row)
    - .planning/phases/13-integration/13-CONTEXT.md (D-12, D-13, D-14)
    - .planning/phases/13-integration/13-PATTERNS.md (CLAUDE.md section patterns)
  </read_first>
  <action>
Three additions to CLAUDE.md:

**Addition 1: Agent Table — add 3 new rows (D-12)**

The Agent Table currently ends with the Brand Verifier row at line 156. Insert 3 new rows before the Brand Verifier row (so they appear in orchestration order — the verifier runs last). Add these rows after the Document Assembler row (line 155) and before the Brand Verifier row (line 156):

```markdown
| design-kit-foundation | Phase 8 assembly complete — launched from Phase 8 Step 5c | workspace/output/client/*.html (specimens to post-process), Phase 7 visual direction | `workspace/output/design-kit/tokens/`, `workspace/output/design-kit/*.html` (copied specimens) | No |
| skill-bundle-packager | Phase 8 assembly complete — launched from Phase 8 Step 5b | workspace/output/client/ outputs (voice guide, brand foundation, practical toolkit) | `workspace/output/skill-bundle/` | No |
| design-kit-packager | design-kit-foundation completes — launched from Phase 8 Step 5d | workspace/output/design-kit/tokens/, workspace/output/design-kit/*.html (foundation output) | `workspace/output/design-kit/components/`, `previews/`, root files | No |
```

Note: design-kit-foundation was supposed to be added in Phase 10 but was not — add it now. The CONTEXT.md D-12 says "design-kit-foundation was added in Phase 10" but grep confirms it was NOT added. All three agents must be added.

Also update the Document Assembler row's Output Location from `workspace/output/[document].md` to `workspace/output/client/[document].md` to reflect the new path structure.

**Addition 2: Workspace Structure section (D-13)**

Insert a new section after the Agent Table section separator (`---` at line 158) and before the `## Background Agent Protocol` heading (line 160). The new section:

```markdown
## Workspace Structure

```
workspace/
├── research/          — Agent research outputs (competitive brief, archetypes, voice fingerprint)
├── drafts/            — Draft content for review (messaging options, visual direction)
├── assets/            — Mark explorations, imagery
└── output/
    ├── client/        — Client brand package (Document Assembler output)
    ├── skill-bundle/  — Agent skill bundle (skill-bundle-packager output)
    └── design-kit/    — Component design kit (design-kit-foundation + design-kit-packager output)
```

---
```

**Addition 3: Multi-Bundle Output section (D-14)**

Insert immediately after the Workspace Structure section (before Background Agent Protocol):

```markdown
## Multi-Bundle Output

Three bundles are produced at end of engagement:

| Bundle | Contents | Downstream Consumer |
|--------|----------|---------------------|
| client/ | brand-foundation.md/html, voice-guide.md/html, color-palette.html, visual-system.html, ui-kit.html, practical-toolkit.md, asset pack specimens | Human client — shareable deliverables |
| skill-bundle/ | SKILL.md, brand-prompt.md, source/ files | Claude Code agents — ingest via skill or system prompt |
| design-kit/ | tokens/*.css/json/js, components/*.html, previews/*.html, landing-page.html, README.md, HANDOFF.md, package.json | Designer/developer — implementation reference |

---
```
  </action>
  <verify>
    <automated>cd /Users/kelseyruger/Projects/a-emporium-working/gold-master-projects/brand-compass && grep -c "skill-bundle-packager\|design-kit-packager\|design-kit-foundation" CLAUDE.md</automated>
    Expected output: at least 3 (one per new agent row)
  </verify>
  <acceptance_criteria>
    - `grep -c "skill-bundle-packager" CLAUDE.md` returns at least 1
    - `grep -c "design-kit-packager" CLAUDE.md` returns at least 1
    - `grep -c "design-kit-foundation" CLAUDE.md` returns at least 1
    - `grep -c "## Workspace Structure" CLAUDE.md` returns 1
    - `grep -c "## Multi-Bundle Output" CLAUDE.md` returns 1
    - `grep -c "skill-bundle/" CLAUDE.md` returns at least 2 (agent table + workspace structure + multi-bundle)
    - `grep -c "design-kit/" CLAUDE.md` returns at least 2
    - `grep "Downstream Consumer" CLAUDE.md` returns 1 (multi-bundle table header)
    - `grep "workspace/output/client/\[document\]" CLAUDE.md` returns at least 1 (updated Document Assembler row)
    - The Agent Table still has the Brand Verifier as the last row
    - `## Background Agent Protocol` heading still exists after the new sections
  </acceptance_criteria>
  <done>CLAUDE.md Agent Table has 12 rows (9 original + 3 new agents); Workspace Structure section shows three-bundle output directory; Multi-Bundle Output section describes each bundle's contents and downstream consumer; Document Assembler output path updated to client/ subdirectory.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries crossed. brand-verifier.md is an agent definition (markdown); CLAUDE.md is project documentation. Neither processes untrusted input directly.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-02 | I (Information Disclosure) | brand-verifier.md | accept | Verifier reads workspace files and reports on them — this is the intended behavior for a local development tool. No secrets involved. |
</threat_model>

<verification>
After both tasks complete:
1. `grep -c "Level 7\|Level 8" .claude/agents/brand-verifier.md` returns at least 6
2. `grep -c "skill-bundle-packager\|design-kit-packager\|design-kit-foundation" CLAUDE.md` returns at least 3
3. `grep -c "## Workspace Structure" CLAUDE.md` returns 1
4. `grep -c "## Multi-Bundle Output" CLAUDE.md` returns 1
</verification>

<success_criteria>
Brand Verifier validates all three bundles with existence + substance checks (Level 7 for skill-bundle, Level 8 for design-kit), both conditional on Phase 8 completion. CLAUDE.md documents all agents in the table, shows the three-bundle workspace structure, and describes what each bundle contains and who consumes it.
</success_criteria>

<output>
After completion, create `.planning/phases/13-integration/13-02-SUMMARY.md`
</output>
