---
phase: 11-agent-skill-bundle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/agents/skill-bundle-packager.md
autonomous: true
requirements:
  - SKIL-01
  - SKIL-02
  - SKIL-03
  - SKIL-04

must_haves:
  truths:
    - "skill-bundle-packager.md exists as a complete agent definition with valid frontmatter (name, description, model: sonnet, tools: Read/Write/Glob/Grep)"
    - "The agent definition contains pre-flight checks that Glob for all 4 required inputs and stop with a clear error message if any are missing"
    - "Task 1 instructions specify verbatim extraction from voice-guide.md Sections 3, 3b, 4, 5, 5b into three source/ files with exact header templates"
    - "Task 2 instructions specify SKILL.md five-section synthesis with translation rule and worked archetype translation example"
    - "Task 2 instructions specify brand-prompt.md 150-300 word composition with priority ordering and explicit no-headings/no-bullets constraint"
    - "The agent handles voice-fingerprint.md conditionally — reads if present, continues without if absent"
    - "language-bank.md merge instructions include deduplication logic retaining practical-toolkit.md version for overlapping entries"
  artifacts:
    - path: ".claude/agents/skill-bundle-packager.md"
      provides: "Complete agent definition for skill bundle generation"
      min_lines: 250
      contains: "model: sonnet"
  key_links:
    - from: ".claude/agents/skill-bundle-packager.md"
      to: "workspace/output/client/voice-guide.md"
      via: "Input section and Task 1 extraction instructions"
      pattern: "voice-guide\\.md"
    - from: ".claude/agents/skill-bundle-packager.md"
      to: "workspace/output/client/brand-foundation.md"
      via: "Input section and Task 2 synthesis instructions"
      pattern: "brand-foundation\\.md"
    - from: ".claude/agents/skill-bundle-packager.md"
      to: "workspace/output/skill-bundle/"
      via: "Output section listing all 5 generated files"
      pattern: "skill-bundle/"
---

<objective>
Create the skill-bundle-packager agent definition file at `.claude/agents/skill-bundle-packager.md`. This agent reads completed brand engagement outputs and produces a complete, usable skill bundle at `workspace/output/skill-bundle/` containing SKILL.md (behavioral voice instructions), brand-prompt.md (persona injection prose), and source/ reference files (voice-rules.md, guardrails.md, language-bank.md).

Purpose: This is the sole deliverable of Phase 11 — a thorough agent definition (250+ lines) that serves as the complete instruction set for a sonnet-model agent to produce skill bundles without any additional context or discovery session.

Output: `.claude/agents/skill-bundle-packager.md`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-agent-skill-bundle/11-CONTEXT.md
@.planning/phases/11-agent-skill-bundle/11-RESEARCH.md
@.planning/phases/11-agent-skill-bundle/11-PATTERNS.md

<interfaces>
<!-- Primary analog: design-kit-foundation.md — exact structural match for frontmatter, identity, input, pre-flight, task structure, output, quality bar -->
<!-- Quality bar analog: document-assembler.md — 200+ line depth and specificity of instruction -->
<!-- Translation rule analog: copywriter.md — "translate, not transcribe" discipline for SKILL.md synthesis -->

From .claude/agents/design-kit-foundation.md (structural pattern):
```markdown
---
name: design-kit-foundation
description: >
  Extracts design tokens from visual-direction.md into standalone CSS/JSON/JS
  files, then post-processes client HTML specimens into design-kit variants
  with external token links.

  **Triggers:**
  - Document Assembler completes client HTML specimens
  - Lead strategist requests design kit generation

model: sonnet
tools: Read, Write, Glob, Grep
---

You are the Design Kit Foundation agent. ...

## Input
- `workspace/drafts/visual-direction.md` -- ... (required)

## Before Starting
Read `workspace/drafts/visual-direction.md` fully. ...

---

### Task 1: Token Extraction
...

---

### Task 2: HTML Post-Processing
...

## Output
- `workspace/output/design-kit/tokens/colors.css`
...

## Quality Bar
- Token files contain real values...
```

From .claude/skills/brand-compass/phase-1-origin/SKILL.md (skill file frontmatter — output SKILL.md format):
```markdown
---
name: phase-1-origin
description: "Phase 1: Origin & Belief"
disable-model-invocation: true
---
```
Note: The generated output SKILL.md uses `name` and `description` only. `disable-model-invocation` is for command-invoked skill files and must NOT be included in the generated output.

From workspace/reference/example-brand/output/voice-guide.md (section headers the agent extracts from):
```
## Section 1: Philosophical Anchor
## Section 2: Voice Summary
## Section 3: Writing Style
## Section 4: Signature Moves
## Section 5: Guardrails
## Section 6: Scaling by Context
```
Conditional sections (present only when voice-fingerprint.md was used during engagement):
- `## Section 3b: Quantitative Voice Markers`
- `## Section 5b: Voice Calibration Table`

From workspace/reference/example-brand/output/practical-toolkit.md (Language Bank structure):
```
## Language Bank
### Phrases We Use
- "Compliance as growth infrastructure"
...
### Phrases We Never Use
- "Leverage" -> Use "build on" or "use"
...
### Say This / Not That
| Instead of... | Say... |
```

From workspace/reference/example-brand/output/brand-foundation.md (section headers for positioning/personality):
```
## Section 3: Positioning
## Section 5: Brand Personality
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the complete skill-bundle-packager agent definition</name>
  <files>.claude/agents/skill-bundle-packager.md</files>

  <read_first>
    .claude/agents/design-kit-foundation.md
    .claude/agents/document-assembler.md
    .claude/agents/copywriter.md
    .planning/phases/11-agent-skill-bundle/11-RESEARCH.md
    .planning/phases/11-agent-skill-bundle/11-PATTERNS.md
    .planning/phases/11-agent-skill-bundle/11-CONTEXT.md
    workspace/reference/example-brand/output/voice-guide.md
    workspace/reference/example-brand/output/practical-toolkit.md
    workspace/reference/example-brand/output/brand-foundation.md
  </read_first>

  <action>
Create `.claude/agents/skill-bundle-packager.md` — a complete agent definition file of 250+ lines following the design-kit-foundation.md structural pattern and the document-assembler.md depth/quality bar. The file has these sections in order:

**1. YAML Frontmatter** (per D-01 of PATTERNS.md Pattern 1):
```yaml
---
name: skill-bundle-packager
description: >
  Reads completed brand engagement outputs and produces a complete, usable
  skill bundle at workspace/output/skill-bundle/. The bundle contains
  SKILL.md (behavioral voice instructions for consuming agents),
  brand-prompt.md (150-300 word persona injection prose), and source/
  reference files (voice-rules.md, guardrails.md, language-bank.md)
  extracted verbatim from the client package.

  **Triggers:**
  - Phase 8 assembly is complete and client outputs exist in workspace/output/client/
  - Lead strategist requests skill bundle generation

model: sonnet
tools: Read, Write, Glob, Grep
---
```

**2. Opening Identity Statement** (per PATTERNS.md Pattern 2):
One paragraph: "You are the Skill Bundle Packager agent. You read completed brand engagement outputs and produce a complete, usable skill bundle at `workspace/output/skill-bundle/`. You run after Phase 8 assembly is complete." Add a second sentence explaining the two-task structure: source extraction first, then synthesis.

**3. `## Input` section** (per PATTERNS.md Pattern 3 and D-10):
Bulleted list with 5 entries:
- `workspace/output/client/voice-guide.md` — primary source for voice definition, signature moves, guardrails, writing style (required)
- `workspace/output/client/brand-foundation.md` — positioning statement, archetype, core belief, personality traits (required)
- `workspace/STATE.md` — brand name and entity type (required)
- `workspace/output/client/practical-toolkit.md` — targeted read for Language Bank / Codify-Retire section only (required for language-bank.md extraction)
- `workspace/research/voice-fingerprint.md` — optional; read if present for brand-prompt.md enrichment and quantitative voice markers; skip if absent

**4. `## Before Starting` section** (per PATTERNS.md Pattern 4 and D-10):
Numbered pre-flight checks 1-5:
1. Glob `workspace/output/client/voice-guide.md` — if not found, stop and report: "voice-guide.md not found at workspace/output/client/. Phase 8 assembly must complete before running skill-bundle-packager."
2. Glob `workspace/output/client/brand-foundation.md` — same stop condition with equivalent message.
3. Glob `workspace/STATE.md` — same stop condition: "STATE.md not found. Cannot determine brand name and entity type."
4. Glob `workspace/output/client/practical-toolkit.md` — same stop condition with equivalent message.
5. Glob `workspace/research/voice-fingerprint.md` — if found, read it now and note the voice profile one-liner and quantitative markers for use in Tasks 1 and 2. If not found, note: "voice-fingerprint.md absent — proceeding without quantitative enrichment." Continue without stopping.

After all checks pass: "Read all required input files now. Read practical-toolkit.md only for the `## Language Bank` section — do not process other sections."

**5. `---` divider then `### Task 1: Source File Extraction`** (per D-07, D-08, D-09):

Opening instruction: "Extract sections verbatim from voice-guide.md and practical-toolkit.md into three files under `workspace/output/skill-bundle/source/`. This is a copy operation — do not rewrite, restructure, or paraphrase the source content. The client-facing prose framing is itself useful instruction for consuming agents."

**File 1: `workspace/output/skill-bundle/source/voice-rules.md`**

Header template:
```markdown
# Voice Rules

*Extracted from voice-guide.md Sections 3 and 4.*
*Direct section copy — no rewriting.*
```

Content: Copy the full text of `## Section 3: Writing Style` (all subsections — Sentence and Paragraph Rhythm, Structure Usage, Authenticity Spectrum). If `## Section 3b: Quantitative Voice Markers` exists, include it verbatim after Section 3. Then copy the full text of `## Section 4: Signature Moves` (all named signature moves with frequency and examples).

**File 2: `workspace/output/skill-bundle/source/guardrails.md`**

Header template:
```markdown
# Guardrails

*Extracted from voice-guide.md Section 5.*
*Direct section copy — no rewriting.*
```

Content: Copy the full text of `## Section 5: Guardrails` (all subsections — Never Do This, Words and Phrases to Avoid, Quality Check). If `## Section 5b: Voice Calibration Table` exists, include it verbatim after Section 5.

**File 3: `workspace/output/skill-bundle/source/language-bank.md`**

Header template:
```markdown
# Language Bank

*Merged from voice-guide.md (phrases to avoid) and practical-toolkit.md (Codify/Retire classification).*
*Duplicates removed — practical-toolkit.md version retained for overlapping entries.*
```

Structure (per D-08, D-09):
```markdown
## Codify (use these)
[Copy from practical-toolkit.md Language Bank > "Phrases We Use" list verbatim]

## Retire (avoid these)
[Merge: Start with practical-toolkit.md "Phrases We Never Use" list. Then check
voice-guide.md Section 5 "Words and Phrases to Avoid" table for any entries NOT
already covered by the practical-toolkit.md list. Add only unique entries from
voice-guide.md. For any word/phrase appearing in BOTH sources, keep only the
practical-toolkit.md version — it has richer substitution guidance.]

## Say This / Not That
[Copy from practical-toolkit.md Language Bank > "Say This / Not That" table verbatim]
```

Include explicit deduplication instruction: "The 'Phrases We Never Use' list in practical-toolkit.md and the 'Words and Phrases to Avoid' table in voice-guide.md Section 5 will overlap (common entries: 'leverage,' 'synergy,' 'best-in-class,' 'turnkey,' 'disrupting'). Retain the practical-toolkit.md version for each overlap. Add only voice-guide.md entries that do not appear in practical-toolkit.md."

Note about the practical-toolkit.md Language Bank format: "practical-toolkit.md may use either the newer Codify/Retire format or the older 'Phrases We Use / Phrases We Never Use' format. Both are valid — copy whichever format exists. Map 'Phrases We Use' to 'Codify (use these)' and 'Phrases We Never Use' to the Retire merge input."

**6. `---` divider then `### Task 2: Bundle Synthesis`**

Opening instruction: "Synthesize two files from the source material: SKILL.md (behavioral voice instructions) and brand-prompt.md (persona injection prose). Read the three source/ files you just created in Task 1, plus brand-foundation.md and STATE.md. If voice-fingerprint.md was found in pre-flight, use its one-liner and quantitative markers to enrich both files."

**Part A: SKILL.md** — `workspace/output/skill-bundle/SKILL.md`

Frontmatter format (per PATTERNS.md Pattern 8):
```markdown
---
name: [brand-slug]-voice
description: >
  Voice and positioning instructions for [Brand Name]. Behavioral writing
  rules for any agent producing content in this brand's voice.
---
```

Where `[brand-slug]` is the brand name from STATE.md lowercased with spaces replaced by hyphens, and `[Brand Name]` is the brand name as-is from STATE.md.

Opening line after frontmatter: "You are writing as [Brand Name]. Everything below defines how this brand sounds, what it says, and what it never does."

Five sections (per D-01, D-02):

**`## Voice Definition`** — Source: voice-guide.md Section 2 (Voice Summary one-liner + Voice Tags) + source/voice-rules.md (Section 3 writing style content). If voice-fingerprint.md present, weave the voice profile one-liner and quantitative markers (average sentence length, vocabulary level, etc.) into this section. Write as behavioral instructions: "Write in short declarative sentences averaging 14-18 words..." NOT rationale: "The brand chose this voice because..."

**`## Signature Moves`** — Source: source/voice-rules.md (Section 4 content). Translate each named technique into "do this when X" behavioral instructions. Example translation: NOT "The brand uses The Plain Translation technique" but "When introducing a technical concept, immediately follow with a plain-English restatement in parentheses or after a dash." Include frequency guidance for each move.

**`## Guardrails`** — Source: source/guardrails.md (Section 5 content). Write as prohibitions with specific examples: "Never use fear as a motivator. Never say 'you need to worry about...' or 'if you don't act now...' — instead, state consequences factually." Include the word substitution table and quality check questions.

**`## Language Bank`** — Source: source/language-bank.md. Reproduce the Codify, Retire, and Say This/Not That sections. This can be closer to verbatim since the language bank is already in instruction format.

**`## Positioning Summary`** — Source: brand-foundation.md Sections 3 (Positioning) + 5 (Brand Personality) + STATE.md (brand name, entity type, what they do). Write as context the consuming agent holds, not discovery rationale. Format: "You serve [audience]. You are the [positioning claim]. You own the territory of [territory description]." NOT: "The brand went through a positioning exercise and identified their territory as..."

Minimum line count: 200 lines. Per D-02, every sentence in SKILL.md should be an actionable instruction or piece of context the consuming agent needs. No strategy backstory, no discovery rationale, no "why we chose this voice" paragraphs.

**THE TRANSLATION RULE** (per D-03 — this is the central design constraint):

Include this explicit instruction block in the agent definition, between the Task 2 opening and Part A:

```markdown
#### The Translation Rule

Discovery material is internal Brand Compass context. SKILL.md is consumed by
an agent that has never seen discovery. You must translate, not transcribe.

Do not copy archetype names (Sage, Rebel, Hero, Caregiver, etc.), territory
labels, or Brand Compass framework terms (Market of One, intersection,
contrarian POV) into SKILL.md sections. For each concept, read the source data
and translate to behavioral output.

**Worked example:**
- Source: brand-foundation.md says "Sage archetype" with traits "precise,
  educational, authoritative"
- BAD output: "You are a Sage archetype. Your territory is compliance as
  growth infrastructure."
- GOOD output: "You communicate as a teaching authority — you share knowledge
  openly, you explain reasoning before conclusions, and you never assert
  credentials without demonstrating them. You treat compliance not as a
  burden but as operational advantage."

**Warning signs of translation failure:** SKILL.md contains any of these words:
archetype, territory, Market of One, intersection, contrarian POV, or any proper
noun coined during discovery phases.
```

**Part B: brand-prompt.md** — `workspace/output/skill-bundle/brand-prompt.md`

Per D-04, D-05, D-06:

Include this instruction block:

"Write brand-prompt.md as 150-300 words of flowing prose — a persona injection that a consuming agent reads as a system prompt prefix. This is NOT a brief, NOT a reference document. No `#` heading characters, no `-` or `*` bullet points. One continuous flowing paragraph or 2-3 flowing paragraphs maximum."

Composition priority order:
1. **Voice + archetype behavioral foundation** (~50-80 words): Open with the archetype character (translated to behavioral description, NOT the archetype name). Follow with 2-3 sentences establishing the core voice feel — what the prose sounds like, what it never does. Source: brand-foundation.md Section 5 (personality) + voice-guide.md Section 2 (voice summary one-liner + voice tags).
2. **Positioning + territory as context** (~40-60 words): 1-2 sentences giving the brand's positioning and territory — written as things the persona "knows" or "believes," not as external description. Source: brand-foundation.md Section 3 (positioning statement + territory).
3. **1-2 personality traits and signature moves** (~30-50 words): The 1-2 most distinctive signature moves translated to behavioral instructions. Source: voice-guide.md Section 4 (top 2 signature moves) + brand-foundation.md Section 5 (top personality traits).
4. **One hard guardrail** (~20-30 words): A single closing "never do" line — the most brand-damaging violation. Source: voice-guide.md Section 5 (first "Never Do This" item).

If voice-fingerprint.md present (per D-12): "Weave the voice profile one-liner and 1-2 quantitative markers into sections 1 and 3 of the composition. Example: '...writes in short declarative bursts — 12-16 words per sentence on average...' This makes the persona more precise and copywriter-executable."

Anti-pattern instruction: "Do NOT write headings, bullets, or structured sections. Do NOT use archetype names. Do NOT list elements mechanically ('Voice: X. Positioning: Y.'). Write as a single flowing persona description a human writer would internalize."

**7. `## Output` section** (per PATTERNS.md Pattern 10):
List all 5 output files:
- `workspace/output/skill-bundle/SKILL.md` — 200+ line behavioral voice skill file with frontmatter
- `workspace/output/skill-bundle/brand-prompt.md` — 150-300 word persona injection prose (no headings, no bullets)
- `workspace/output/skill-bundle/source/voice-rules.md` — Sections 3 (+3b if present) and 4 from voice-guide.md, verbatim
- `workspace/output/skill-bundle/source/guardrails.md` — Section 5 (+5b if present) from voice-guide.md, verbatim
- `workspace/output/skill-bundle/source/language-bank.md` — merged and deduplicated language bank from voice-guide.md and practical-toolkit.md

**8. `## Quality Bar` section** (per PATTERNS.md Pattern 11):
- SKILL.md is 200+ lines
- SKILL.md contains exactly five `## ` section headers: Voice Definition, Signature Moves, Guardrails, Language Bank, Positioning Summary
- SKILL.md contains zero occurrences of: "archetype", "territory" (as a Brand Compass label), "Market of One", "intersection", "contrarian POV"
- SKILL.md frontmatter has `name` and `description` fields — does NOT have `disable-model-invocation`
- brand-prompt.md is 150-300 words
- brand-prompt.md contains zero `#` characters and zero bullet markers (`-` at line start or `*` at line start)
- source/voice-rules.md contains verbatim text from voice-guide.md Sections 3 and 4 — not paraphrased
- source/guardrails.md contains verbatim text from voice-guide.md Section 5 — not paraphrased
- source/language-bank.md has three subsections: Codify, Retire, Say This / Not That
- source/language-bank.md has no duplicated entries between the Retire list and merged voice-guide.md phrases-to-avoid content
  </action>

  <verify>
    <automated>test -f .claude/agents/skill-bundle-packager.md && echo "EXISTS" || echo "MISSING"; wc -l .claude/agents/skill-bundle-packager.md; grep -c "^---$" .claude/agents/skill-bundle-packager.md; grep -c "model: sonnet" .claude/agents/skill-bundle-packager.md; grep -c "tools: Read, Write, Glob, Grep" .claude/agents/skill-bundle-packager.md</automated>
  </verify>

  <acceptance_criteria>
    - `.claude/agents/skill-bundle-packager.md` exists
    - File is 250+ lines (wc -l returns >= 250)
    - File contains exactly `model: sonnet` (grep -c returns 1)
    - File contains exactly `tools: Read, Write, Glob, Grep` (grep -c returns 1)
    - File contains `name: skill-bundle-packager` in frontmatter
    - File contains `## Input` section with 5 bulleted entries (voice-guide.md, brand-foundation.md, STATE.md, practical-toolkit.md, voice-fingerprint.md)
    - File contains `## Before Starting` with 5 numbered pre-flight checks
    - File contains `### Task 1: Source File Extraction` with three file output specs (voice-rules.md, guardrails.md, language-bank.md)
    - File contains `### Task 2: Bundle Synthesis` with SKILL.md and brand-prompt.md instructions
    - File contains `#### The Translation Rule` section with worked archetype translation example
    - File contains `## Output` section listing 5 output files
    - File contains `## Quality Bar` section with structural checks
    - grep for `voice-fingerprint.md` returns matches in Before Starting (conditional check) and Task 2 (enrichment usage)
    - grep for `## Codify` and `## Retire` returns matches in Task 1 language-bank.md instructions
    - grep for `deduplicat` returns at least 1 match (deduplication instruction present)
    - No grep match for `disable-model-invocation` in the SKILL.md output template section
  </acceptance_criteria>

  <done>
    skill-bundle-packager.md is a complete 250+ line agent definition following design-kit-foundation.md structure and document-assembler.md quality bar. All 12 user decisions (D-01 through D-12) are implemented as specific agent instructions. The file defines frontmatter, identity, inputs, pre-flight checks, Task 1 (source extraction with 3 file specs), Task 2 (bundle synthesis with translation rule, SKILL.md five-section template, brand-prompt.md composition algorithm), output listing, and quality bar.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify agent definition against reference examples and patterns</name>
  <files>.claude/agents/skill-bundle-packager.md</files>

  <read_first>
    .claude/agents/skill-bundle-packager.md
    .claude/agents/design-kit-foundation.md
    workspace/reference/example-brand/output/voice-guide.md
    workspace/reference/example-brand/output/practical-toolkit.md
  </read_first>

  <action>
Read the completed skill-bundle-packager.md and verify it against the structural pattern and content requirements. Run these checks and fix any issues found:

**Structural checks against design-kit-foundation.md pattern:**
1. Frontmatter block starts and ends with `---`, contains `name`, `description`, `model: sonnet`, `tools: Read, Write, Glob, Grep`, and `**Triggers:**` in description
2. Opening identity paragraph exists immediately after frontmatter close
3. `## Input` section exists with bulleted file paths using backtick + ` -- ` + description format
4. `## Before Starting` section exists with numbered pre-flight checks
5. `### Task 1:` and `### Task 2:` sections exist with `---` dividers between them
6. `## Output` section exists with bulleted output file paths
7. `## Quality Bar` section exists with specific pass/fail checks

**Content checks against CONTEXT.md decisions:**
- D-01: SKILL.md template shows all 5 sections (Voice Definition, Signature Moves, Guardrails, Language Bank, Positioning Summary)
- D-02: Instructions say "enforceable behavioral rules" or equivalent — NOT "rationale paragraphs"
- D-03: Translation Rule section exists with worked example showing BAD (archetype name) vs GOOD (behavioral instruction) output
- D-04: brand-prompt.md instructions say "voice-and-archetype-led" or equivalent composition approach
- D-05: Priority order documented: (1) voice+archetype, (2) positioning+territory, (3) traits+signature moves, (4) one guardrail
- D-06: Explicit constraint against headings and bullets in brand-prompt.md
- D-07: Task 1 instructions say "verbatim" or "direct section copy" — NOT "rewrite" or "restructure"
- D-08: Extraction mapping matches: voice-rules.md from Sections 3+4, guardrails.md from Section 5, language-bank.md merged from voice-guide.md + practical-toolkit.md
- D-09: language-bank.md merge instructions include deduplication logic
- D-10: Input section lists exactly 3 required + 1 targeted + 1 conditional file
- D-11: No mention of reading `workspace/drafts/` or `workspace/research/` files other than voice-fingerprint.md
- D-12: voice-fingerprint.md enrichment instructions mention "one-liner" and "quantitative markers"

**Source structure cross-reference:**
- Verify that voice-guide.md section numbers referenced in Task 1 match the actual section headers in the reference file: `## Section 3: Writing Style`, `## Section 4: Signature Moves`, `## Section 5: Guardrails`
- Verify that practical-toolkit.md section referenced in Task 1 matches: `## Language Bank` with subsections `### Phrases We Use`, `### Phrases We Never Use`, `### Say This / Not That`
- Verify that brand-foundation.md sections referenced in Task 2 include: `## Section 3: Positioning`, `## Section 5: Brand Personality`

If any check fails, fix the issue in skill-bundle-packager.md directly. After all fixes, re-read the file to confirm all checks pass.
  </action>

  <verify>
    <automated>grep -c "## Input" .claude/agents/skill-bundle-packager.md && grep -c "## Before Starting" .claude/agents/skill-bundle-packager.md && grep -c "### Task 1" .claude/agents/skill-bundle-packager.md && grep -c "### Task 2" .claude/agents/skill-bundle-packager.md && grep -c "## Output" .claude/agents/skill-bundle-packager.md && grep -c "## Quality Bar" .claude/agents/skill-bundle-packager.md && grep -c "Translation Rule" .claude/agents/skill-bundle-packager.md && grep -c "voice-fingerprint" .claude/agents/skill-bundle-packager.md && grep -c "deduplicat" .claude/agents/skill-bundle-packager.md && echo "ALL SECTIONS PRESENT"</automated>
  </verify>

  <acceptance_criteria>
    - grep "## Input" returns 1 match
    - grep "## Before Starting" returns 1 match
    - grep "### Task 1" returns 1 match
    - grep "### Task 2" returns 1 match
    - grep "## Output" returns 1 match
    - grep "## Quality Bar" returns 1 match
    - grep "Translation Rule" returns at least 1 match
    - grep "voice-fingerprint" returns 3+ matches (Input, Before Starting, Task 2)
    - grep "deduplicat" returns at least 1 match
    - grep "verbatim" returns at least 1 match (source extraction is verbatim copy)
    - grep "Section 3: Writing Style" returns at least 1 match (correct section reference)
    - grep "Section 4: Signature Moves" returns at least 1 match
    - grep "Section 5: Guardrails" returns at least 1 match
    - grep "brand-foundation.md" returns at least 2 matches (Input + Task 2)
    - grep "Codify" returns at least 1 match in language-bank.md instructions
    - grep "150-300 word" or "150-300" appears in brand-prompt.md instructions
    - grep "disable-model-invocation" returns 0 matches (must not appear)
    - wc -l returns 250+ lines for the complete file
  </acceptance_criteria>

  <done>
    The agent definition passes all structural checks (matches design-kit-foundation.md pattern), all content checks (implements all 12 decisions D-01 through D-12), and all source cross-reference checks (correct voice-guide.md section numbers, practical-toolkit.md section names, brand-foundation.md section references). No `disable-model-invocation` field present in SKILL.md output template. File is 250+ lines.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent definition -> runtime execution | The agent definition is static text; it runs in a Claude Code sandbox with Read/Write/Glob/Grep only. No network access, no shell commands, no code execution. |
| Client workspace -> skill bundle output | The agent reads from workspace/output/client/ (trusted, generated by Document Assembler) and writes to workspace/output/skill-bundle/ (same trust level). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Information Disclosure | skill-bundle-packager.md reads STATE.md which may contain client details | accept | STATE.md is local workspace data, not transmitted externally. Agent operates in local sandbox only. No PII leaves the machine. |
| T-11-02 | Tampering | Agent definition could be modified after creation | accept | File is in .claude/agents/ under git version control. Standard file integrity through git. Low-value target — no credentials, no auth, no network access. |
| T-11-03 | Denial of Service | Missing input files cause agent to fail | mitigate | Pre-flight checks (Before Starting section) Glob for all 4 required files and stop with clear error message if any are missing. Graceful skip for optional voice-fingerprint.md. |
</threat_model>

<verification>
1. `ls .claude/agents/skill-bundle-packager.md` — file exists
2. `wc -l .claude/agents/skill-bundle-packager.md` — 250+ lines
3. `grep "model: sonnet" .claude/agents/skill-bundle-packager.md` — correct model
4. `grep "tools: Read, Write, Glob, Grep" .claude/agents/skill-bundle-packager.md` — correct tools
5. `grep -c "^## " .claude/agents/skill-bundle-packager.md` — has Input, Before Starting, Output, Quality Bar sections
6. `grep -c "^### Task" .claude/agents/skill-bundle-packager.md` — has Task 1 and Task 2
7. `grep "Translation Rule" .claude/agents/skill-bundle-packager.md` — translation rule present
8. `grep "voice-fingerprint" .claude/agents/skill-bundle-packager.md` — conditional input handling present
9. `grep "deduplicat" .claude/agents/skill-bundle-packager.md` — deduplication logic present
10. `grep "disable-model-invocation" .claude/agents/skill-bundle-packager.md` — returns 0 matches (NOT present)
</verification>

<success_criteria>
- skill-bundle-packager.md exists at .claude/agents/ with 250+ lines
- Agent definition follows design-kit-foundation.md structural pattern exactly (frontmatter, identity, input, pre-flight, tasks, output, quality bar)
- All 12 user decisions (D-01 through D-12) are implemented as specific instructions within the agent definition
- Task 1 specifies verbatim extraction of voice-guide.md Sections 3/3b/4/5/5b into source/ files with exact header templates
- Task 1 specifies language-bank.md merge with deduplication logic retaining practical-toolkit.md version for overlaps
- Task 2 specifies SKILL.md five-section synthesis with Translation Rule and worked archetype-to-behavioral-instruction example
- Task 2 specifies brand-prompt.md 150-300 word composition with priority ordering and no-headings/no-bullets constraint
- Pre-flight checks Glob for all 4 required inputs with stop-on-missing behavior, plus conditional voice-fingerprint.md check
- Quality Bar includes structural checks: line count, section count, translation rule enforcement, word count, bullet check
</success_criteria>

<output>
After completion, create `.planning/phases/11-agent-skill-bundle/11-01-SUMMARY.md`
</output>
