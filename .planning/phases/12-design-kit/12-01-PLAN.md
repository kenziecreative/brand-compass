---
phase: 12-design-kit
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/agents/design-kit-packager.md
autonomous: true
requirements:
  - DKIT-01
  - DKIT-02
  - DKIT-03
  - DKIT-04
  - DKIT-05

must_haves:
  truths:
    - "design-kit-packager.md exists as a valid agent definition with sonnet model and Read/Write/Glob/Grep tools"
    - "Agent definition contains pre-flight checks for all 6 required inputs with stop-on-missing behavior"
    - "Agent definition specifies all 7 atomized component HTML files with Component Personality annotations"
    - "Agent definition specifies all 5 preview card HTML files with min-width/overflow constraints"
    - "Agent definition specifies landing-page.html with entity-type conditional reading Type field from STATE.md"
    - "Agent definition specifies README.md, package.json, HANDOFF.md root files"
    - "Agent definition uses ../../tokens/ paths for subdirectory files and ../tokens/ for root-level files"
  artifacts:
    - path: ".claude/agents/design-kit-packager.md"
      provides: "Complete agent definition for design kit packaging"
      min_lines: 300
  key_links:
    - from: ".claude/agents/design-kit-packager.md"
      to: "workspace/output/design-kit/tokens/*.css"
      via: "pre-flight Glob checks + link tag instructions"
      pattern: "../../tokens/"
    - from: ".claude/agents/design-kit-packager.md"
      to: "workspace/drafts/visual-direction.md"
      via: "Section 8 Component Personality read instruction"
      pattern: "Component Personality"
    - from: ".claude/agents/design-kit-packager.md"
      to: "workspace/output/client/brand-foundation.md"
      via: "Section 4 Messaging Architecture read instruction"
      pattern: "brand-foundation.md"
---

<objective>
Create the complete design-kit-packager agent definition at `.claude/agents/design-kit-packager.md`. This agent reads design-kit-foundation outputs (tokens, post-processed specimens) and visual-direction.md Component Personality section, then produces atomized component HTML files, preview cards, root documentation files, and a landing-page.html specimen.

Purpose: Enable Brand Compass to generate a full design kit (components, previews, documentation, landing page) from completed brand engagement outputs — the visual half of the multi-bundle output system.

Output: Single agent definition file (~300-350 lines) following the skill-bundle-packager.md structural pattern at document-assembler.md depth of instruction.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-design-kit/12-CONTEXT.md
@.planning/phases/12-design-kit/12-RESEARCH.md
@.planning/phases/12-design-kit/12-PATTERNS.md
@.claude/agents/skill-bundle-packager.md
@.claude/agents/design-kit-foundation.md

<interfaces>
<!-- Key structural patterns the executor must replicate -->

From .claude/agents/skill-bundle-packager.md (structural analog):
- YAML frontmatter: name, description (multi-line with > scalar + **Triggers:**), model: sonnet, tools: Read, Write, Glob, Grep
- Opening identity paragraph immediately after frontmatter
- ## Input section with (required)/(optional) qualifiers
- ## Before Starting with numbered Glob checks + stop/continue branching
- ### Task N: headers separated by --- dividers
- ## Output listing all files produced
- ## Quality Bar with named pass/fail checks

From .claude/agents/design-kit-foundation.md (token link pattern):
- Files at design-kit root: href="../tokens/[file].css"
- Files in subdirectories: href="../../tokens/[file].css"
- No :root {} blocks allowed in externally-linked HTML files

From 12-PATTERNS.md Pattern 5 (STATE.md entity type field):
- Field is **Type:** under ## Client section (NOT entity_type)
- Example value: "Accounting & Compliance Services"
- Individual keywords: individual, artist, creator, solo, freelance, photographer, musician, writer, coach, consultant
- Org keywords: agency, studio, firm, company, services, organization, business, nonprofit, startup, brand
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write complete design-kit-packager agent definition</name>
  <files>.claude/agents/design-kit-packager.md</files>
  <read_first>
    - .claude/agents/skill-bundle-packager.md (structural analog — replicate frontmatter, identity, input, pre-flight, task, output, quality bar pattern)
    - .claude/agents/design-kit-foundation.md (token link pattern — ../tokens/ vs ../../tokens/ distinction; input section format)
    - .planning/phases/12-design-kit/12-PATTERNS.md (complete content specification for every section of the agent definition)
    - .planning/phases/12-design-kit/12-RESEARCH.md (component personality table, preview card specs, landing page entity-type conditional, anti-patterns)
    - .planning/phases/12-design-kit/12-CONTEXT.md (all 12 locked decisions D-01 through D-12)
  </read_first>
  <action>
Write `.claude/agents/design-kit-packager.md` as a complete agent definition (~300-350 lines) with the following structure. Use skill-bundle-packager.md as the structural template and 12-PATTERNS.md as the content specification.

**Section 1: YAML Frontmatter (copy from PATTERNS.md Pattern 1 exactly):**
```yaml
---
name: design-kit-packager
description: >
  Reads design-kit-foundation outputs (tokens, post-processed specimens) and
  visual-direction.md Component Personality section, then produces atomized
  component HTML files, preview cards, root documentation files, and a new
  landing-page.html specimen at workspace/output/design-kit/. Runs after
  design-kit-foundation completes.

  **Triggers:**
  - design-kit-foundation agent has completed (tokens and post-processed specimens exist)
  - Lead strategist requests design kit packaging

model: sonnet
tools: Read, Write, Glob, Grep
---
```

**Section 2: Opening Identity (from PATTERNS.md Pattern 2):**
State the agent name, what it reads, what it produces, its dependency on design-kit-foundation, and the four-task sequential structure.

**Section 3: ## Input (from PATTERNS.md Pattern 3):**
List all 7 inputs (6 required + 1 optional mark-explorations). Each with backtick path, dash, description, and (required)/(optional) qualifier. Include the explicit D-12 constraint: "read ONLY for Section 8 Component Personality; do not read for token values."

**Section 4: ## Before Starting (from PATTERNS.md Pattern 4):**
7 numbered pre-flight Glob checks:
1. `workspace/output/design-kit/tokens/colors.css` — stop if missing: "design-kit-foundation has not run. tokens/colors.css not found. Run design-kit-foundation before design-kit-packager."
2. `workspace/output/design-kit/tokens/typography.css` — stop with same pattern
3. `workspace/output/design-kit/tokens/spacing.css` — stop with same pattern
4. `workspace/drafts/visual-direction.md` — stop: "visual-direction.md not found. Component Personality cannot be read without this file."
5. `workspace/output/client/brand-foundation.md` — stop: "brand-foundation.md not found. Landing page requires real brand copy."
6. `workspace/STATE.md` — stop: "STATE.md not found. Entity type required for landing page conditional."
7. `workspace/assets/mark-explorations/` — optional: if found note first mark path; if not found note "No mark asset found — brand-groups.html will use text-based lockup." Continue without stopping.

After checks pass instruction: Read visual-direction.md Section 8 Component Personality only. Read brand-foundation.md Section 4 Messaging Architecture (tagline, boilerplate, proof points). Read STATE.md Client section for **Type:** field. Explicitly state: "Do NOT read visual-direction.md for token values — those are already in tokens/*.css files."

**Section 5: ### Task 1: Atomized Components (per D-01, D-02, D-03):**
Write 7 component HTML files to `workspace/output/design-kit/components/`. Create the directory if it doesn't exist.

State the HTML structure pattern:
- `<!DOCTYPE html>` with charset, viewport, title
- Google Fonts link if font requires it (read font name from typography.css)
- Token links: `<link rel="stylesheet" href="../../tokens/colors.css">`, `../../tokens/typography.css`, `../../tokens/spacing.css` (TWO levels up because components/ is a subdirectory)
- `<style>` block with CSS using `var()` references annotated with `/* [TOKEN: --var-name] */` for token-sourced properties and `/* [BRAND PERSONALITY: reason] */` for Component Personality decisions
- Body with rendered component examples and a usage note section
- NO `:root {}` blocks — all custom property values come from external token files

List all 7 components as numbered File entries (#### File 1 through #### File 7) with these specifications from RESEARCH.md Pattern 4 table:

| File | Component | Primary Personality Source |
|------|-----------|---------------------------|
| File 1 | button.html | Solid fill, moderate radius; teal primary, border-only secondary; hover = color shift |
| File 2 | card.html | Flat with 1px border + subtle shadow; left border accent on feature variants |
| File 3 | form-field.html | Inherit radius-sm; focus = accent color; error = emphasis/coral |
| File 4 | nav.html | Brand primary background; body font semibold for active; horizontal primary |
| File 5 | modal.html | Shadow-lg; radius-lg; overlay dims background |
| File 6 | alert.html | Four variants (info/success/warning/error); left border accent; badge-shape containers |
| File 7 | badge.html | 4px radius (radius-sm); slightly rounded rectangle; structured not casual |

For each component entry, state: (1) which Component Personality property to read from visual-direction.md section 8, (2) what brand-specific behavior to implement beyond token references, (3) the CSS annotation requirement (`/* [TOKEN] */` vs `/* [BRAND PERSONALITY] */`).

Include the explicit instruction: "Read the actual Component Personality decisions from this client's visual-direction.md section 8. The table above shows the EXAMPLE brand's decisions. Map whatever personality decisions exist in the actual file."

**Section 6: ### Task 2: Preview Cards and Root Files (per D-04, D-05, D-06, DKIT-04):**

**Preview Cards (5 files):** Write to `workspace/output/design-kit/previews/`. Create directory if needed.

State the CSS constraints from RESEARCH.md Pattern 5:
- `.preview-card { min-width: 150px; max-width: 400px; width: 100%; border: 1px solid var(--color-border); border-radius: var(--radius-md); overflow: hidden; }`
- `.preview-zone { padding: var(--space-md); }` — visual content area
- `.token-layer { border-top: 1px solid var(--color-border); padding: var(--space-sm) var(--space-md); font-family: var(--font-mono); font-size: 0.7rem; display: flex; flex-wrap: wrap; gap: var(--space-xs); overflow: hidden; }` — per D-05 floor behavior
- `.token-chip { white-space: nowrap; min-width: 0; }`
- Token links: `<link rel="stylesheet" href="../../tokens/colors.css">` (TWO levels up)

List all 5 preview cards (#### File 1 through File 5) with content specs from RESEARCH.md Pattern 5 table:

| File | Preview Zone Content | Token Layer Values |
|------|---------------------|-------------------|
| colors.html | Color swatches for all palette colors; semantic token rows | --color-primary, --color-action, --color-border |
| type.html | Font specimens at 3 sizes (display, body, mono); weight scale | --font-display, --font-body, --font-weight-heading |
| spacing.html | Visual scale bars at xs/sm/md/lg/xl/2xl; radius dots | --space-md, --radius-md, --transition-speed |
| components.html | Mini rendered button, badge, card, form input | --color-action, --radius-sm, --shadow-sm |
| brand-groups.html | Brand lockup (text in display font or mark if found) + tagline + primary swatch | --font-display, --color-primary, brand slug |

For brand-groups.html: instruct the agent to Glob `workspace/assets/mark-explorations/` — if a mark file is found, embed it; otherwise use text-based lockup (brand name in `var(--font-display)` + tagline).

**Root Files (3 files):** Write to `workspace/output/design-kit/` root.

#### File 6: README.md
- Written in brand voice (pull tone from tagline/core belief in brand-foundation.md)
- Title: "[Brand Name] Design Kit"
- Brand voice opening (NOT a generic "Welcome to the design kit" opener)
- "What's in this kit" section with directory listing
- "How to use" quick start
- Source brand voice from brand-foundation.md

#### File 7: package.json
- Valid JSON structure:
```json
{
  "name": "[brand-slug]-design-kit",
  "version": "1.0.0",
  "description": "Design tokens and component library for [Brand Name]",
  "private": true,
  "brand": {
    "name": "[Brand Name]",
    "generated": "[ISO date]"
  }
}
```
- Brand slug = brand name from STATE.md lowercased, spaces replaced with hyphens

#### File 8: HANDOFF.md
- Designer quick start (open specimens, token locations, Figma/Sketch import via DTCG)
- Developer quick start (tailwind.config.js import, CSS custom properties)
- Component inventory (all 7 files and contents)
- Preview card index (all 5 files)
- Token file index (what each covers)
- "Read next" pointing to skill-bundle SKILL.md

**Section 7: ### Task 3: Landing Page (per D-07, D-08, D-09, DKIT-05):**
Write `workspace/output/design-kit/landing-page.html`.

Token links: `<link rel="stylesheet" href="../tokens/colors.css">` (ONE level up — landing-page.html is at design-kit root, not in a subdirectory).

**Entity-type conditional (from PATTERNS.md Pattern 11, corrected field name):**

Instruct the agent to read `**Type:**` from `workspace/STATE.md` Client section (NOT entity_type — that field does not exist).

Classification:
- Individual keywords (individual, artist, creator, solo, freelance, photographer, musician, writer, coach, consultant) -> Individual template
- Org keywords (agency, studio, firm, company, services, organization, business, nonprofit, startup, brand) -> Org template
- Absent or ambiguous -> Org template (default)

**Individual template:**
- Hero: `<h1>` = tagline (Section 4 > Tagline), subhead = one-sentence boilerplate (Section 4 > Boilerplate > one-sentence), single CTA
- Content: core narrative as flowing prose (Section 4 > Core Narrative), proof points as personal statements (Section 4 > Proof Points), connect/contact section

**Org template:**
- Hero: `<h1>` = tagline, subhead = one-sentence boilerplate, feature grid (3-4 proof points as cards), dual CTA (primary + secondary "learn more")
- Content: core narrative as brand narrative, about/mission section

State explicitly: "Use REAL copy from brand-foundation.md. No placeholder text, no lorem ipsum, no [bracket] markers."

Copy sourcing paths:
- Hero headline: Section 4: Messaging Architecture > Tagline
- Subheadline: Section 4 > Boilerplate Descriptions > one-sentence variant
- Body: Section 4 > Core Narrative
- Feature grid / proof points: Section 4 > Proof Points

**Section 8: ## Output (from PATTERNS.md Pattern 12):**
List all 16 output files grouped by subdirectory (Components 7, Preview Cards 5, Root files 4).

Add explicit "DO NOT write" note: must not write to `workspace/output/design-kit/brand-foundation.html` or `workspace/output/design-kit/color-palette.html` — those are owned by design-kit-foundation.

**Section 9: ## Quality Bar (from PATTERNS.md Pattern 13):**
Named pass/fail checks:
- **All 7 component HTML files exist** and contain no `:root {}` declarations
- **Component link tags use ../../tokens/** (not ../tokens/)
- **All 5 preview cards contain min-width: 150px** and overflow: hidden on token-layer
- **Preview link tags use ../../tokens/** (not ../tokens/)
- **landing-page.html contains no [bracket] placeholder text** and no lorem ipsum
- **landing-page.html link tags use ../tokens/** (one level, not two)
- **package.json parses as valid JSON**
- **README.md is written in brand voice** — no generic "Welcome to" opener; first paragraph reflects the brand's tone
- **brand-foundation.html and color-palette.html at design-kit root are NOT overwritten** — if either file exists before this agent runs, it must still exist unchanged after
- **All var() references in components resolve to token file variables** — no inline value declarations; no hardcoded hex colors in component styles (except for #fff or #000 used as contrast text)
- **CSS annotation present on every declaration** in component style blocks: either `/* [TOKEN: --var-name] */` or `/* [BRAND PERSONALITY: reason] */`
  </action>
  <verify>
    <automated>grep -c "^---$" .claude/agents/design-kit-packager.md | grep -q "2" && grep -q "name: design-kit-packager" .claude/agents/design-kit-packager.md && grep -q "model: sonnet" .claude/agents/design-kit-packager.md && grep -q "tools: Read, Write, Glob, Grep" .claude/agents/design-kit-packager.md && grep -q "## Input" .claude/agents/design-kit-packager.md && grep -q "## Before Starting" .claude/agents/design-kit-packager.md && grep -q "## Output" .claude/agents/design-kit-packager.md && grep -q "## Quality Bar" .claude/agents/design-kit-packager.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - .claude/agents/design-kit-packager.md exists
    - File contains exactly 2 lines matching "^---$" (YAML frontmatter delimiters)
    - File contains "name: design-kit-packager"
    - File contains "model: sonnet"
    - File contains "tools: Read, Write, Glob, Grep"
    - File contains "## Input" section header
    - File contains "## Before Starting" section header
    - File contains "### Task 1" (components)
    - File contains "### Task 2" (previews + root files) OR "### Task 3" depending on numbering
    - File contains "## Output" section header
    - File contains "## Quality Bar" section header
    - File contains "../../tokens/" (subdirectory link pattern)
    - File contains "../tokens/" (root-level link pattern)
    - File contains "Component Personality"
    - File contains "brand-foundation.md"
    - File contains "**Type:**" (correct STATE.md field name, not "entity_type")
    - File contains "landing-page.html"
    - File contains all 7 component names: button.html, card.html, form-field.html, nav.html, modal.html, alert.html, badge.html
    - File contains all 5 preview names: colors.html, type.html, spacing.html, components.html, brand-groups.html
    - File contains "README.md", "package.json", "HANDOFF.md"
    - File is 300+ lines (wc -l >= 300)
    - File contains "[TOKEN:" annotation pattern
    - File contains "[BRAND PERSONALITY:" annotation pattern
    - File contains "DO NOT" prohibition regarding brand-foundation.html / color-palette.html overwriting
    - File does NOT contain "entity_type" as a field name reference (only **Type:** is valid)
  </acceptance_criteria>
  <done>Complete design-kit-packager agent definition exists at .claude/agents/design-kit-packager.md with valid frontmatter, opening identity, input section, pre-flight checks, 3-4 sequential tasks covering all 16 output files, output listing, and quality bar. All 12 CONTEXT.md decisions (D-01 through D-12) are implemented as specific instructions. The file is 300+ lines.</done>
</task>

<task type="auto">
  <name>Task 2: Verify agent definition against all requirements and decisions</name>
  <files>.claude/agents/design-kit-packager.md</files>
  <read_first>
    - .claude/agents/design-kit-packager.md (the file just created — verify its contents)
    - .planning/phases/12-design-kit/12-CONTEXT.md (verify all 12 decisions are implemented)
  </read_first>
  <action>
Read the completed agent definition and verify each CONTEXT.md decision is implemented:

**D-01 check:** Grep for "Component Personality" — must appear in task instructions for components. Components must reference visual-direction.md section 8.
**D-02 check:** Grep for "[TOKEN:" and "[BRAND PERSONALITY:" — annotation instruction must be present.
**D-03 check:** Grep for "../../tokens/" — components must link external token files via this path.
**D-04 check:** Grep for "preview-card" or "mini reference card" — preview cards use the mini reference pattern.
**D-05 check:** Grep for "min-width: 150px" and "overflow: hidden" — floor behavior is specified.
**D-06 check:** Count occurrences of preview file names (colors.html, type.html, spacing.html, components.html, brand-groups.html) — all 5 must be present.
**D-07 check:** Grep for "brand-foundation.md" in landing page section — real copy sourcing is specified. Grep for "lorem ipsum" or "placeholder" as prohibition language.
**D-08 check:** Grep for "Individual" and "Org" template — entity-type conditional exists with both branches.
**D-09 check:** Grep for "../tokens/" in landing page section — external token links specified at root level.
**D-10 check:** Grep for "design-kit-foundation" — dependency on foundation agent is stated.
**D-11 check:** Verify frontmatter has name: design-kit-packager, model: sonnet, tools: Read, Write, Glob, Grep.
**D-12 check:** Grep for explicit prohibition of reading visual-direction.md for token values — instruction must say "only Section 8" or equivalent.

**Additional structural checks:**
- Verify line count is 300+ (wc -l)
- Verify no occurrence of "entity_type" as a field reference (must use **Type:** per PATTERNS.md correction)
- Verify "DO NOT write" / "DO NOT overwrite" instruction for brand-foundation.html and color-palette.html
- Verify all 7 component file names are listed in Output section
- Verify all 5 preview file names are listed in Output section
- Verify README.md, package.json, HANDOFF.md are listed in Output section
- Verify landing-page.html is listed in Output section

If any check fails, fix the agent definition file to address the gap. Document fixes made.
  </action>
  <verify>
    <automated>wc -l < .claude/agents/design-kit-packager.md | awk '{if ($1 >= 300) print "PASS: "$1" lines"; else print "FAIL: only "$1" lines"}'</automated>
  </verify>
  <acceptance_criteria>
    - wc -l output shows 300 or more lines
    - grep -c "Component Personality" .claude/agents/design-kit-packager.md returns 2 or more
    - grep -c "\[TOKEN:" .claude/agents/design-kit-packager.md returns 1 or more
    - grep -c "\[BRAND PERSONALITY:" .claude/agents/design-kit-packager.md returns 1 or more
    - grep -c "../../tokens/" .claude/agents/design-kit-packager.md returns 3 or more (one per token file in component/preview instructions)
    - grep -c "min-width: 150px" .claude/agents/design-kit-packager.md returns 1 or more
    - grep -c "overflow: hidden" .claude/agents/design-kit-packager.md returns 1 or more
    - grep "entity_type" .claude/agents/design-kit-packager.md returns 0 matches (field does not exist)
    - grep -c "\\*\\*Type:\\*\\*" .claude/agents/design-kit-packager.md returns 1 or more (correct field name)
    - grep -c "Individual" .claude/agents/design-kit-packager.md returns 1 or more (template branch)
    - grep -c "Org" .claude/agents/design-kit-packager.md returns 1 or more (template branch)
    - grep -c "landing-page.html" .claude/agents/design-kit-packager.md returns 2 or more
    - grep -c "brand-foundation.md" .claude/agents/design-kit-packager.md returns 2 or more
  </acceptance_criteria>
  <done>All 12 CONTEXT.md decisions verified as implemented in the agent definition. Line count is 300+. No structural gaps, no incorrect field names, no missing output files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent write scope | Agent must only write to workspace/output/design-kit/ subdirectories |
| Agent read scope | Agent reads from workspace/ paths only (no system files) |
| HTML output | Generated HTML must not include script injection vectors |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | design-kit-packager write paths | mitigate | Agent definition Output section lists all 16 allowed output paths explicitly; Quality Bar includes "DO NOT write" prohibition for foundation-owned files |
| T-12-02 | Information Disclosure | HTML output could include sensitive paths | accept | Agent only writes to workspace/output/ (non-sensitive project workspace); no secrets or credentials involved |
| T-12-03 | Elevation of Privilege | Agent definition could instruct arbitrary Bash execution | mitigate | Agent tools restricted to "Read, Write, Glob, Grep" in frontmatter — no Bash/shell access available |
| T-12-04 | Tampering | HTML components could include inline scripts | mitigate | specimen-design SKILL.md constraint ("Pre-render what you can. Save JavaScript for genuinely interactive features") applies; agent definition specifies "HTML + CSS only" with no script tags in component/preview templates |
</threat_model>

<verification>
Phase 12 is complete when:
1. `.claude/agents/design-kit-packager.md` exists with 300+ lines
2. Valid YAML frontmatter with name, description, model: sonnet, tools: Read, Write, Glob, Grep
3. All 12 CONTEXT.md decisions (D-01 through D-12) are traceable to specific instructions in the file
4. All 16 output files are listed in the ## Output section
5. Pre-flight checks cover all 6 required inputs with stop-on-missing behavior
6. Token link paths are correct: ../../tokens/ for subdirectory files, ../tokens/ for root-level
7. Entity-type conditional reads **Type:** field (not entity_type) from STATE.md Client section
8. Quality Bar contains named pass/fail checks covering all key defect patterns
</verification>

<success_criteria>
- Agent definition file exists at .claude/agents/design-kit-packager.md
- File is 300+ lines with valid frontmatter
- All DKIT-01 through DKIT-05 requirements are covered by instructions in the file
- Running `grep -q "name: design-kit-packager" .claude/agents/design-kit-packager.md` exits 0
- Running `grep -q "model: sonnet" .claude/agents/design-kit-packager.md` exits 0
- No occurrences of "entity_type" as a field name (uses **Type:** instead)
</success_criteria>

<output>
After completion, create `.planning/phases/12-design-kit/12-01-SUMMARY.md`
</output>
