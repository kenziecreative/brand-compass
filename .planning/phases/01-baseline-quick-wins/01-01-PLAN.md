---
phase: 01-baseline-quick-wins
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - feature-support/
  - PROJECT-WRITEUP.md
autonomous: true
requirements:
  - BASE-01

must_haves:
  truths:
    - "All previously uncommitted tracked changes are committed to git as a clean baseline"
    - "The feature-support/ research archive is preserved in git history"
    - "PROJECT-WRITEUP.md is relocated into the research archive before commit"
    - "feature-support/ is gitignored after its initial commit so future local changes stay local"
    - "Remote is updated to match local as master copy"
  artifacts:
    - path: ".gitignore"
      provides: "Ignores feature-support/ after baseline commit"
      contains: "feature-support/"
  key_links:
    - from: "PROJECT-WRITEUP.md"
      to: "feature-support/research/source-material/"
      via: "git mv before commit"
      pattern: "feature-support/research/source-material/PROJECT-WRITEUP.md"
---

<objective>
Commit all existing uncommitted local changes as a clean baseline before applying any quick-win edits.

Purpose: Establishes a known-good starting point. The tracked file changes (Phase 1/3 conditionals, checkpoint-a, brand-verifier, document-assembler, STATE-template, example-brand outputs) and untracked research archive (feature-support/, PROJECT-WRITEUP.md) need to be in git history before we start editing the same files for quick wins.

Output: Two baseline commits in git, remote force-pushed to overwrite trivial README commits.
</objective>

<execution_context>
@/Users/kelseyruger/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kelseyruger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-baseline-quick-wins/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit tracked changes as baseline</name>
  <files>.claude/agents/brand-verifier.md, .claude/agents/document-assembler.md, .claude/commands/brand-compass/checkpoint-a.md, .claude/commands/brand-compass/phase-1-origin.md, .claude/commands/brand-compass/phase-3-positioning.md, CLAUDE.md, package.json, workspace/STATE-template.md, workspace/reference/example-brand/output/brand-foundation.html, workspace/reference/example-brand/output/brand-foundation.md</files>
  <action>
Stage and commit all currently tracked modified files (the 10 files shown by `git status` with `M` status). These are the Phase 1 values/mission/vision conditionals, Phase 3 service definition conditionals, updated checkpoint-a, brand-verifier, document-assembler, STATE-template, and example-brand outputs.

Commit message: "feat: add business/org conditional outputs for Phase 1 and Phase 3" (or similar — Claude's discretion on exact wording per CONTEXT.md).

Do NOT stage untracked files yet — those go in Task 2.
  </action>
  <verify>
    <automated>cd /Users/kelseyruger/Projects/a-emporium-working/gold-master-projects/brand-compass && git log --oneline -1 | grep -q "feat" && git status --short | grep -v "^??" | wc -l | grep -q "^0$" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>All 10 tracked modified files are committed. `git status` shows no staged/unstaged tracked changes — only untracked files remain.</done>
</task>

<task type="auto">
  <name>Task 2: Commit research archive, gitignore feature-support, force-push to remote</name>
  <files>PROJECT-WRITEUP.md, feature-support/, .gitignore</files>
  <action>
1. Move PROJECT-WRITEUP.md into the research archive: `git mv` won't work since it's untracked, so use regular `mv PROJECT-WRITEUP.md feature-support/research/source-material/PROJECT-WRITEUP.md`.

2. Stage and commit the entire feature-support/ directory (research archive with 7 research phases, 35 sources, 37 cross-reference patterns): "docs: archive feature research materials"

3. Add `feature-support/` to .gitignore so future local changes to the research directory stay local. Stage and commit .gitignore: "chore: gitignore feature-support research archive"

4. Force-push to remote to overwrite the 3 trivial README commits. The remote is https://github.com/kenziecreative/brand-compass. Local is the master copy per CONTEXT.md decision. Command: `git push --force origin main`

Note: The force-push is explicitly authorized per CONTEXT.md locked decisions ("force-push to remote to overwrite the 3 trivial README commits — local is master copy").
  </action>
  <verify>
    <automated>cd /Users/kelseyruger/Projects/a-emporium-working/gold-master-projects/brand-compass && test -f feature-support/research/source-material/PROJECT-WRITEUP.md && grep -q "feature-support" .gitignore && git status --short | wc -l | grep -q "^0$" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>PROJECT-WRITEUP.md relocated into research archive. feature-support/ committed then gitignored. Remote matches local. Working tree is clean — ready for quick-win edits.</done>
</task>

</tasks>

<verification>
- `git log --oneline -5` shows the baseline commits
- `git status` shows clean working tree
- `git diff HEAD~3..HEAD --stat` shows all previously modified files plus archive
- `test -f feature-support/research/source-material/PROJECT-WRITEUP.md` confirms relocation
- `grep "feature-support" .gitignore` confirms gitignore entry
</verification>

<success_criteria>
All uncommitted local changes are committed as a clean baseline. Working tree is clean. Remote is updated. The codebase is ready for quick-win file edits in Plans 02 and 03.
</success_criteria>

<output>
After completion, create `.planning/phases/01-baseline-quick-wins/01-01-SUMMARY.md`
</output>
