---
phase: 05-process-refinements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/commands/brand-compass/start.md
  - workspace/STATE-template.md
  - CLAUDE.md
autonomous: true
requirements: [PROC-01, PROC-02]

must_haves:
  truths:
    - "When a client describes multiple distinct offerings during onboarding, the facilitator asks whether they are one brand or separate brands"
    - "The brand architecture decision is stored in STATE.md and the template has the field"
    - "STATE.md template has a Client Dynamic section with interaction style, pushback calibration, emotional moments, and voice notes fields"
    - "The pushback audit reads the client's calibration level and adapts its self-check accordingly"
  artifacts:
    - path: ".claude/commands/brand-compass/start.md"
      provides: "Brand architecture conditional question in onboarding"
      contains: "one brand"
    - path: "workspace/STATE-template.md"
      provides: "Brand Architecture field and Client Dynamic section"
      contains: "Brand Architecture"
    - path: "CLAUDE.md"
      provides: "Phase 0 reference to brand architecture conditional; calibration-aware pushback audit"
      contains: "calibration"
  key_links:
    - from: ".claude/commands/brand-compass/start.md"
      to: "workspace/STATE-template.md"
      via: "Brand Architecture field populated during onboarding"
      pattern: "Brand Architecture"
    - from: "CLAUDE.md"
      to: "workspace/STATE-template.md"
      via: "Pushback audit reads pushback calibration level"
      pattern: "pushback calibration"
---

<objective>
Add brand architecture detection for multi-offer clients and client dynamic state tracking to the system.

Purpose: Clients with multiple offerings need the system to surface a brand architecture question early (Phase 0) so the engagement stays focused on one brand. Client dynamic state gives the facilitator a private notebook to track interaction patterns, calibrate pushback, and accumulate voice signal across sessions.

Output: Updated start.md with brand architecture conditional, updated STATE-template.md with Brand Architecture field and Client Dynamic section, updated CLAUDE.md with Phase 0 reference and calibration-aware pushback audit.
</objective>

<execution_context>
@/Users/kelseyruger/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kelseyruger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-process-refinements/05-CONTEXT.md
@.claude/commands/brand-compass/start.md
@workspace/STATE-template.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add brand architecture conditional and client dynamic state fields</name>
  <files>.claude/commands/brand-compass/start.md, workspace/STATE-template.md, CLAUDE.md</files>
  <action>
**start.md changes:**

In Group 1 (Who Are You?), after question 3 ("In a few sentences, what do you do?"), add a conditional instruction block:

```
**Brand Architecture Check:**
After the client describes what they do, if they mention multiple distinct offerings, services, or products that could conceivably be separate brands (e.g., "I run a consulting firm and also have a skincare line"), surface this question naturally:

"You mentioned [X] and [Y]. Are these all part of one brand, or do some of them need their own identity?"

- If **one brand:** Proceed normally. Note in STATE.md Brand Architecture: "Single brand — all offerings under [Brand Name]"
- If **separate brands:** Ask which brand to develop first. Record in STATE.md Brand Architecture: "Multiple — developing: [X], noted for future: [Y], [Z]". The system develops one brand at a time.
- If the client describes a single clear offering, skip this question entirely — do not force a brand architecture discussion when there is nothing to discuss.
```

This should feel like a natural follow-up to "what do you do," not a technical architecture question. The trigger is the client mentioning multiple offerings — not every client gets this question.

**STATE-template.md changes:**

1. Add a `Brand Architecture` field to the Client section, after the `Asset Packs` line:
```
- **Brand Architecture:** [Single brand / Multiple — developing: X, noted for future: Y, Z]
```

2. Add a new `## Client Dynamic` section after the `## Client` section (before `## Current Position`):
```
## Client Dynamic

*Updated at phase transitions. Facilitator's private notebook — not shared with client.*

- **Interaction Style:** [How the client communicates — e.g., "verbose, prefers options over open questions, thinks out loud"]
- **Pushback Calibration:** [Level + reasoning — e.g., "medium-high — responds well to direct challenge but needs the reasoning spelled out first"]
- **Emotional Moments:**
  [Timestamped list of moments where the client lit up, got defensive, or showed strong energy]
  - [Phase X] [moment description]
- **Voice Notes:**
  [Running list of raw quotes, distinctive vocabulary, recurring metaphors — feeds Voice Analyzer]
  - "[exact quote]" — [context]
```

**CLAUDE.md changes:**

1. In the Phase 0 summary (around line 111-126), after "Visual preference instincts" bullet, add:
```
- Brand architecture check (conditional — only when multiple offerings mentioned)
```

2. Update the Pushback Audit section (around line 61-69). Keep the existing structure but make step 3 calibration-aware. Replace the current step 3 with:

```
3. **If no pushback occurred:** Read the Pushback Calibration level from STATE.md Client Dynamic section (if populated). Then identify one area where the client's answer was too safe, too generic, or too comfortable. Challenge it now — before closing the phase.
   - **High calibration clients:** Be direct. "Before we move on — [element] is too safe. Here's what's missing: [specific gap]."
   - **Low calibration clients:** Lead with reasoning. "Before we move on, I want to explore one thing further: [element]. Here's why it matters: [reasoning]. Can we push it further?"
   - **Unknown/not yet calibrated:** Use the default: "Before we move on, I want to push on one thing: [specific element]. It feels [too safe / too generic / too similar to competitors]. [Specific concern]. Can we sharpen it?"
```

3. In the State Management section "When to Write State" list, add a bullet:
```
- Phase transition — consolidate Client Dynamic observations (interaction style, pushback calibration, emotional moments, voice notes) from discovery notes into STATE.md Client Dynamic section
```

4. In the Passive Voice Capture section (around line 85-93), after the paragraph about "Voice Notes" section in discovery notes, add a sentence:
```
At phase transitions, consolidate voice notes into STATE.md Client Dynamic > Voice Notes for cross-session persistence.
```
  </action>
  <verify>
    <automated>grep -c "Brand Architecture" workspace/STATE-template.md && grep -c "Client Dynamic" workspace/STATE-template.md && grep -c "Brand Architecture" .claude/commands/brand-compass/start.md && grep -c "calibration" CLAUDE.md</automated>
  </verify>
  <done>start.md contains brand architecture conditional in Group 1; STATE-template.md has Brand Architecture field in Client section and full Client Dynamic section with all 4 fields; CLAUDE.md Phase 0 summary mentions brand architecture check; pushback audit is calibration-aware with three calibration paths; state management references Client Dynamic updates at phase transitions</done>
</task>

</tasks>

<verification>
- `grep "Brand Architecture" workspace/STATE-template.md` returns the field definition
- `grep "Client Dynamic" workspace/STATE-template.md` returns the section header
- `grep "one brand" .claude/commands/brand-compass/start.md` confirms the conditional question
- `grep "calibration" CLAUDE.md` confirms the pushback audit update
- `grep "brand architecture" CLAUDE.md` confirms Phase 0 summary update
</verification>

<success_criteria>
- Brand architecture conditional fires only when multiple offerings are mentioned — not every client
- Client Dynamic section reads like a facilitator's notebook, not a clinical form
- Pushback audit adapts to three calibration levels (high, low, unknown)
- All STATE-template fields follow existing format conventions (Field Name: [description])
</success_criteria>

<output>
After completion, create `.planning/phases/05-process-refinements/05-01-SUMMARY.md`
</output>
