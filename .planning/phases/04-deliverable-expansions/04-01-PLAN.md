---
phase: 04-deliverable-expansions
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
  - .claude/agents/visual-director.md
autonomous: true
requirements: [DLVR-01]

must_haves:
  truths:
    - "Phase 7 discovery includes 2-3 motion-specific questions before the Visual Director launch"
    - "Visual Director generates a motion direction section as part of visual-direction.md"
    - "Each motion recommendation includes its reduced-motion alternative inline"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Motion discovery questions in Phase 7"
      contains: "Motion Direction"
    - path: ".claude/agents/visual-director.md"
      provides: "Motion direction output section in Visual Director"
      contains: "Motion Direction"
  key_links:
    - from: "CLAUDE.md Phase 7"
      to: ".claude/agents/visual-director.md"
      via: "Motion answers feed Visual Director input"
      pattern: "motion"
---

<objective>
Add motion direction to the Phase 7 visual pipeline — discovery questions in CLAUDE.md and output specification in the Visual Director agent.

Purpose: DLVR-01 requires motion direction as a new deliverable. The discovery-to-agent pipeline must be established before the Document Assembler can render motion into HTML (handled in Plan 03).
Output: CLAUDE.md Phase 7 with motion questions, Visual Director with motion output section.
</objective>

<execution_context>
@/Users/kelseyruger/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kelseyruger/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-deliverable-expansions/04-CONTEXT.md
@.planning/phases/03-output-quality/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add motion discovery questions to CLAUDE.md Phase 7</name>
  <files>CLAUDE.md</files>
  <action>
In CLAUDE.md Phase 7 (Visual Identity), add a "Motion Direction" question block AFTER the "Application" questions (questions 24-25) and BEFORE the "Launch prompt." Number them as 26-28 continuing the existing sequence. Questions should capture:

1. (Q26) Interaction feel — "Should interactions feel snappy and immediate, or smooth and easing? When someone clicks a button or opens a menu, what's the pace?"
2. (Q27) Movement appropriateness — "How much movement is appropriate for your brand? Think of a spectrum from 'almost nothing moves' to 'things glide and animate frequently.'"
3. (Q28) Transition personality — "When content appears or changes, should it feel like a quick cut (instant, decisive) or a dissolve (gradual, flowing)?"

Add a section header "Motion:" before these questions, matching the existing pattern (Color:, Typography:, Mark/Logo:, Imagery:, Application:).

Update the Visual Director launch prompt to mention motion: change "personality, archetypes, brand world — into visual direction" to "personality, archetypes, brand world — into visual direction including motion principles."

Update "Phase complete when" to include "motion direction defined" — change to: "Visual direction approved, color palette finalized, typography selected, mark created, imagery style defined, motion direction defined."
  </action>
  <verify>
    <automated>grep -n "Motion" CLAUDE.md | head -10 && grep -c "motion" CLAUDE.md</automated>
  </verify>
  <done>CLAUDE.md Phase 7 contains 3 motion discovery questions numbered 26-28, launch prompt references motion, and phase completion criteria includes motion direction.</done>
</task>

<task type="auto">
  <name>Task 2: Add motion direction output section to Visual Director</name>
  <files>.claude/agents/visual-director.md</files>
  <action>
In .claude/agents/visual-director.md, make these changes:

1. In the "## Your Task" numbered list, add item 9: "Define motion direction — principles, default timing/easing values, and reduced-motion fallbacks"

2. In the "## Output Format" template (the markdown block inside the code fence), add a new section AFTER "## AI Image Generation Prompt" and BEFORE "## Design System Parameters":

```
---

## Motion Direction

### Motion Principles
[2-3 principles governing how motion expresses the brand personality. Each should resolve a motion design decision — e.g., "Movement is purposeful, never decorative — every animation communicates a state change."]

1. **[Principle]:** [What it means for animation decisions]
2. **[Principle]:** [What it means]
3. **[Principle]:** [What it means]

### Default Values

**Transitions (hover, focus, color change):**
- Duration: [Xms] — [rationale]
- Easing: [CSS easing function] — [why this curve]
- Reduced motion: [alternative — e.g., "instant, no transition"]

**Entrances (elements appearing, modals opening):**
- Duration: [Xms] — [rationale]
- Easing: [CSS easing function]
- Reduced motion: [alternative — e.g., "instant appear, no animation"]

**Exits (elements leaving, modals closing):**
- Duration: [Xms] — [rationale]
- Easing: [CSS easing function]
- Reduced motion: [alternative]

**Micro-interactions (button press, toggle, checkbox):**
- Duration: [Xms]
- Easing: [CSS easing function]
- Reduced motion: [alternative]

### What to Avoid
[Specific motion patterns that would be wrong for this brand — e.g., "No bouncy/elastic animations — they introduce playfulness that conflicts with the brand's measured authority."]
```

3. In the "## Quality Bar" section at the end, add a bullet: "**Motion serves personality:** Timing and easing choices reflect the brand's energy level and temperament. A calm, deliberate brand gets longer durations and gentle easing. An energetic, snappy brand gets short durations and sharp curves. Every value includes its reduced-motion alternative."

4. In the Design System Parameters CSS Custom Properties Block template, add motion tokens after `--transition-speed`:

```css
--transition-easing: [value];
--entrance-duration: [value];
--entrance-easing: [value];
--exit-duration: [value];
--exit-easing: [value];
```

Note: the existing `--transition-speed` property already exists in the CSS block. Keep it and add the new properties after it.
  </action>
  <verify>
    <automated>grep -n "Motion Direction\|motion\|entrance-duration\|exit-duration\|Reduced motion" .claude/agents/visual-director.md | head -15</automated>
  </verify>
  <done>Visual Director agent includes Motion Direction in task list, output format contains Motion Direction section with principles/defaults/reduced-motion/avoid, CSS properties block includes motion tokens, and quality bar references motion.</done>
</task>

</tasks>

<verification>
Both files updated. Motion pipeline flows: Phase 7 discovery questions (CLAUDE.md) -> client answers -> Visual Director input -> motion direction output in visual-direction.md. Reduced-motion alternatives appear inline with each recommendation per user decision.
</verification>

<success_criteria>
- CLAUDE.md Phase 7 has motion discovery questions 26-28
- Visual Director output template includes Motion Direction section
- Every motion default value placeholder includes a reduced-motion alternative
- CSS Custom Properties Block includes motion tokens
</success_criteria>

<output>
After completion, create `.planning/phases/04-deliverable-expansions/04-01-SUMMARY.md`
</output>
